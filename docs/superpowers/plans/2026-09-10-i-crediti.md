# I crediti, e la prima generazione (B2) — piano di lavoro

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Chi apre lo studio può comprare crediti, vedere il prezzo prima di premere, generare un'immagine con Nano Banana Pro usando i propri asset come riferimento, e riavere i soldi se fallisce.

**Architecture:** L'aritmetica del prezzo è **pura** e sta in `src/engine/ledger.js` e `src/engine/listino.js`, importati **dal browser e dal Worker**: il preventivo e l'addebito sono letteralmente la stessa funzione, quindi non possono divergere. Il saldo vero vive in Postgres e si muove solo con due funzioni SQL atomiche; il browser ne tiene uno specchio in sola lettura che arriva da `/me`.

**Tech Stack:** React 19, `node --test`, Cloudflare Workers (fetch + Cron Trigger), Supabase (Postgres + RLS), Stripe Checkout, Google Gemini 3 Pro Image.

**Spec:** [docs/superpowers/specs/2026-09-10-i-crediti-design.md](../specs/2026-09-10-i-crediti-design.md)

---

## Global Constraints

- **`npm test && npm run build` verdi prima di ogni commit.**
- **`git add <percorsi>`, mai `git add -A`.**
- **Un commit per decisione**, col perché nel corpo.
- **Il saldo è in MILLESIMI di euro, interi.** Mai centesimi, mai virgola mobile.
- **`Math.round` per il margine, mai `Math.ceil`** — spec § 5.
- **`MARGIN = 0.14`**, applicato al costo del fornitore.
- **Nessuna cifra di prezzo nel browser che non venga da `listino.js`.**
- **Ogni chiave i18n va in `it.json` e `en.json`**, o un test fallisce.
- **«asset», mai «lavoro/work/piece»** nelle stringhe — un test lo caccia.
- **La libreria non si chiude mai** (spec B1 § 3.5).
- **Nessuna chiave segreta nel browser né in un commit.** Solo `wrangler secret put`.
- **Il preventivo si vede PRIMA del tasto**, in ogni servizio. È la promessa pubblicata, non una preferenza.

---

## Prima di cominciare — quello che deve fare il committente

1. **Una chiave API Google** con accesso a `gemini-3-pro-image`, da
   [aistudio.google.com](https://aistudio.google.com). Serve al **Task 0**, che
   è un cancello: senza, il piano non parte.
2. **Le tabelle su Supabase** (Task 2, il SQL è scritto per intero).
3. **Il segreto** `GOOGLE_API_KEY` con `npx wrangler secret put`.

Nessun prodotto nuovo su Stripe: i pacchetti si creano al volo con `price_data`.

---

## File Structure

| file | responsabilità | task |
|---|---|---|
| **Create** `docs/2026-09-10-misura-nbp.md` | Quanto ci mette davvero il fornitore. | 0 |
| **Move** `src/store/ledger.js` → `src/engine/ledger.js` | L'aritmetica del prezzo. Pura, di tutt'e due. | 1 |
| **Create** `src/engine/listino.js` | Costo e limiti di ogni servizio a pagamento. | 1 |
| **Rewrite** `test/ledger.test.js` | Millesimi, margine dichiarato, niente float. | 1 |
| **Create** `test/listino.test.js` | Il preventivo e l'addebito non divergono. | 1 |
| **Create** `docs/2026-09-10-schema-b2.sql` | Le tabelle e le due funzioni atomiche. | 2 |
| **Modify** `worker/eventi.js` | Distinguere ricarica da abbonamento. | 3 |
| **Modify** `worker/index.js` | `/ricarica`, `/genera`, `scheduled`. | 3, 4, 5 |
| **Create** `worker/fornitori/google.js` | L'adattatore di Nano Banana Pro. | 4 |
| **Create** `worker/conto.js` | Addebito, accredito, lavori. Un posto solo. | 4 |
| **Modify** `wrangler.jsonc` | Il Cron Trigger. | 5 |
| **Modify** `src/servizi/index.js` | `serve` nel descrittore, `servizioAperto`. | 6 |
| **Create** `src/servizi/immagine.js` | Il servizio «Immagine», dichiarato. | 7 |
| **Create** `src/components/Preventivo.jsx` | Il prezzo prima del tasto. | 7 |
| **Create** `src/components/Riferimenti.jsx` | Gli Elements, presi dalla libreria. | 7 |
| **Create** `src/components/Ricarica.jsx` | I tre pacchetti. | 8 |
| **Modify** `src/landing/copy.js` | Le tre frasi che cambiano. | 9 |

---

## Task 0: Misurare il fornitore — è un cancello, non un preambolo

**Files:** Create `docs/2026-09-10-misura-nbp.md`

**Interfaces:**
- Consumes: `GOOGLE_API_KEY` dal committente.
- Produces: un numero, e la decisione se il § 6.3 della spec regge.

> ⚠️ **Perché prima di tutto.** `/genera` è progettato come **una richiesta
> sola**: chiedi, aspetti, ricevi. Regge finché il fornitore risponde in meno di
> trenta secondi. Le fonti pubbliche dicono 5–20 s, ma **non sono una misura
> nostra**, e sono quasi certamente su prompt semplici senza riferimenti.
> Scoprire a Task 4 finito che ci vogliono quaranta secondi vuol dire rifare
> `/genera` in due tempi con tutto il resto già attaccato sopra.

- [ ] **Step 1: Misurare, col caso peggiore**

Non un prompt di due parole: **cinque immagini di riferimento**, che è il caso
che il prodotto deve reggere. Da terminale, con la chiave in una variabile
d'ambiente e mai in un file:

```bash
cat > /tmp/misura-nbp.mjs <<'EOF'
const CHIAVE = process.env.GOOGLE_API_KEY;
const URL = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-3-pro-image:generateContent';

// Cinque riferimenti finti ma veri come byte: PNG 8x8 rossi.
const PNG = 'iVBORw0KGgoAAAANSUhEUgAAAAgAAAAICAYAAADED76LAAAAFElEQVR42mP8z8BQz0AEYBxVSF+FABJADveWkH6oAAAAAElFTkSuQmCC';
const parti = [
  { text: 'Un pupazzo di pezza che tiene una tazza, luce morbida, sfondo panna.' },
  ...Array.from({ length: 5 }, () => ({ inlineData: { mimeType: 'image/png', data: PNG } })),
];

const tempi = [];
for (let i = 0; i < 5; i++) {
  const t0 = Date.now();
  const r = await fetch(`${URL}?key=${CHIAVE}`, {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({ contents: [{ parts: parti }] }),
  });
  const ms = Date.now() - t0;
  const corpo = await r.text();
  tempi.push(ms);
  console.log(`giro ${i + 1}: ${r.status} in ${ms} ms  ${r.ok ? '' : corpo.slice(0, 200)}`);
}
tempi.sort((a, b) => a - b);
console.log(`\nmediana ${tempi[2]} ms · peggiore ${tempi[4]} ms`);
console.log(tempi[2] < 20000 ? 'OK: /genera resta una richiesta sola.' : 'NO: serve il giro in due tempi.');
EOF
GOOGLE_API_KEY='...' node /tmp/misura-nbp.mjs
```

- [ ] **Step 2: Scrivere cosa è venuto fuori**

Creare `docs/2026-09-10-misura-nbp.md` con: la mediana, la peggiore, lo stato
HTTP, e **la forma esatta della risposta** (dove sta l'immagine nel JSON, e se
il corpo dice quanti token ha consumato — serve a `costo_reale` al Task 4).

- [ ] **Step 3: Il cancello**

- mediana **< 20 s** → il piano prosegue com'è scritto;
- mediana **≥ 20 s** → **fermarsi e dirlo al committente.** Il § 6.3 della spec
  va rifatto in due tempi (`POST /genera` risponde subito con un id,
  `GET /genera/:id` ritira) prima di scrivere i Task 4 e seguenti.

- [ ] **Step 4: Commit**

```bash
git add docs/2026-09-10-misura-nbp.md
git commit -m "$(cat <<'EOF'
docs: quanto ci mette davvero Nano Banana Pro, con cinque riferimenti

Misurato invece che letto: le fonti pubbliche dicono 5-20 s ma su prompt
semplici, e il caso che questo prodotto deve reggere e' cinque immagini di
riferimento. E' il numero che decide se `/genera` puo' essere una richiesta
sola: scoprirlo dopo aver scritto tutto sopra vorrebbe dire rifarlo.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 1: L'aritmetica del prezzo, in millesimi

**Files:** Move `src/store/ledger.js` → `src/engine/ledger.js`; Create `src/engine/listino.js`, `test/listino.test.js`; Rewrite `test/ledger.test.js`

**Interfaces:**
- Consumes: niente. È la base.
- Produces:
  - `MARGIN: 0.14`, `mils(euro) => intero`, `toEuro(millesimi) => number`,
    `priceFor(costoMillesimi) => { total, cost, margin }`,
    `formatEuro(millesimi, lang) => string` in `src/engine/ledger.js`;
  - `LISTINO`, `prezzoDi(servizio) => { total, cost, margin }`,
    `limitiDi(servizio) => { personaggio, oggetto, stile, totale }` in
    `src/engine/listino.js`.

- [ ] **Step 1: Scrivere i test che falliscono**

Sostituire **tutto** `test/ledger.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { MARGIN, mils, toEuro, priceFor, formatEuro } from '../src/engine/ledger.js';

/*
 * Il denaro si conta in MILLESIMI di euro, interi.
 *
 * Non in centesimi, e la ragione non e' la precisione: e' l'onesta'. Il
 * margine e' il 14% del COSTO, cioe' il 12,3% del prezzo. Arrotondato al
 * centesimo su cifre piccole non lo e' piu': su un costo di 12 centesimi il
 * margine diventa 2, cioe' il 14,3%. E questo prodotto si vende scrivendo
 * «di ogni euro, Zack ne rimette 12 centesimi su di se'».
 *
 * Un numero pubblicato che non e' quello che incassi non e' un arrotondamento:
 * e' una frase falsa sulla propria home.
 *
 * E non in virgola mobile, mai: 0,1 + 0,2 fa 0,30000000000000004, e su denaro
 * vero quell'errore si accumula finche' qualcuno spende un millesimo che non
 * ha. E' il motivo per cui le banche non usano i float.
 */

test('gli euro diventano millesimi interi', () => {
  assert.equal(mils(1), 1000);
  assert.equal(mils(0.14), 140);
  assert.equal(mils(0.1) + mils(0.2), 300, 'in float farebbe 0,30000000000000004');
  assert.equal(Number.isInteger(mils(2.99)), true);
  assert.equal(mils(2.99), 2990);
});

test('il margine dichiarato e’ quello incassato', () => {
  // 123 millesimi e' il costo vero di un'immagine Nano Banana Pro (§ 6.1).
  const p = priceFor(123);
  assert.equal(p.cost, 123);
  assert.equal(p.margin, 17, '14% di 123 fa 17,22, arrotondato al piu’ vicino 17');
  assert.equal(p.total, 140);

  const quota = p.margin / p.total;
  assert.ok(quota > 0.115 && quota < 0.128, `il margine e’ il ${(quota * 100).toFixed(1)}% invece del 12%`);
});

test('⚠️ al centesimo la stessa cosa mentirebbe', () => {
  /*
   * La prova che il cambio di unita' serviva. Al centesimo il costo e' 12, il
   * margine arrotondato per eccesso e' 2, e il totale 14: il 14,3% invece del
   * 12,3. Questo test non chiede al codice di fare qualcosa — descrive il
   * difetto che il codice adesso non ha, perche' fra sei mesi qualcuno
   * proporra' di tornare ai centesimi «che sono piu' semplici».
   */
  const alCentesimo = { cost: 12, margin: Math.ceil(12 * MARGIN), total: 12 + Math.ceil(12 * MARGIN) };
  assert.equal(alCentesimo.margin / alCentesimo.total > 0.14, true);
  const alMillesimo = priceFor(123);
  assert.equal(alMillesimo.margin / alMillesimo.total < 0.128, true);
});

test('arrotonda al PIU’ VICINO, non per eccesso', () => {
  // `ceil` al millesimo arrotonda su 0,1 centesimi ogni volta e rigonfia il
  // margine dichiarato. `round` sbaglia al massimo di 0,05 centesimi, e
  // sbaglia in tutt'e due i versi.
  assert.equal(priceFor(100).margin, 14, '14% di 100 fa esattamente 14');
  assert.equal(priceFor(101).margin, 14, '14,14 arrotonda a 14, non a 15');
  assert.equal(priceFor(110).margin, 15, '15,4 arrotonda a 15');
});

test('un costo storto non produce un prezzo', () => {
  // Un prezzo inventato e' peggio di un errore: si addebita.
  for (const brutto of [-1, NaN, Infinity, null, undefined, '12', {}]) {
    assert.throws(() => priceFor(brutto), `«${JSON.stringify(brutto)}» ha prodotto un prezzo`);
  }
});

test('i millesimi si scrivono come euro veri', () => {
  assert.equal(formatEuro(140, 'it'), '0,14 €');
  assert.equal(formatEuro(5000, 'it'), '5,00 €');
  assert.equal(formatEuro(140, 'en'), '€0.14');
  assert.equal(toEuro(140), 0.14);
});
```

Creare `test/listino.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { LISTINO, prezzoDi, limitiDi } from '../src/engine/listino.js';
import { priceFor } from '../src/engine/ledger.js';

/*
 * Il preventivo e l'addebito leggono la STESSA tabella.
 *
 * E' la § 3.1 della spec di B1, ed e' la promessa piu' fragile del prodotto:
 * la home scrive «ogni generazione ti dice quanto costa prima che tu prema».
 * Se il numero mostrato e quello addebitato vengono da due posti, prima o poi
 * divergono — e il giorno che divergono hai mentito a un cliente che ti aveva
 * creduto sulla parola.
 *
 * Qui non divergono perche' sono la stessa funzione. Questi test difendono che
 * resti cosi'.
 */

test('ogni voce di listino ha un prezzo, e viene da priceFor', () => {
  for (const [id, voce] of Object.entries(LISTINO)) {
    assert.deepEqual(prezzoDi(id), priceFor(voce.costo), `${id} calcola il prezzo per conto suo`);
  }
});

test('un servizio che non e’ a listino non ha un prezzo inventato', () => {
  assert.throws(() => prezzoDi('inventato'), /inventato/);
  assert.throws(() => prezzoDi(undefined));
});

test('il margine dichiarato vale per OGNI voce, non solo per la prima', () => {
  // Il giorno che si aggiunge Seedance o ElevenLabs, questo test guarda anche
  // loro senza che nessuno si ricordi di aggiungerlo.
  for (const id of Object.keys(LISTINO)) {
    const { margin, total } = prezzoDi(id);
    const quota = margin / total;
    assert.ok(quota > 0.115 && quota < 0.128, `${id}: margine al ${(quota * 100).toFixed(1)}%`);
  }
});

test('i limiti dei riferimenti vengono dal listino, non da una costante', () => {
  const l = limitiDi('immagine-nbp');
  assert.equal(l.totale, 14);
  assert.equal(l.personaggio, 5);
  assert.equal(l.oggetto, 6);
  assert.equal(l.stile, 3);
  // Un servizio che non dichiara riferimenti non ne accetta nessuno, invece di
  // mandarli a un fornitore che non sa cosa farsene.
  assert.deepEqual(limitiDi('inventato'), { personaggio: 0, oggetto: 0, stile: 0, totale: 0 });
});

test('la somma dei ruoli non supera il totale', () => {
  // 5 + 6 + 3 = 14. Se un giorno non tornasse, si accetterebbe una richiesta
  // che il fornitore rifiuta, dopo aver addebitato.
  for (const id of Object.keys(LISTINO)) {
    const l = limitiDi(id);
    assert.ok(l.personaggio + l.oggetto + l.stile <= l.totale, `${id}: i ruoli superano il totale`);
  }
});
```

- [ ] **Step 2: Far girare — devono fallire**

```bash
node --test test/ledger.test.js test/listino.test.js 2>&1 | grep -E "^not ok|Cannot find" | head
```

Atteso: `Cannot find module '../src/engine/ledger.js'`.

- [ ] **Step 3: Spostare e potare il registro**

```bash
git mv src/store/ledger.js src/engine/ledger.js
```

Sostituire **tutto** il contenuto di `src/engine/ledger.js`:

```js
/**
 * L'aritmetica del prezzo. Pura, e **di tutt'e due**: la importa il browser
 * per il preventivo e il Worker per l'addebito.
 *
 * È la § 3.1 della spec di B1 — «il preventivo e l'addebito leggono la stessa
 * tabella» — resa impossibile da violare: non sono due tabelle che si
 * assomigliano, è la stessa funzione.
 *
 * **Tutto in millesimi di euro, interi.** Non centesimi: il margine è il 14%
 * del costo, cioè il 12,3% del prezzo, e arrotondato al centesimo su cifre
 * piccole diventa il 14,3% — un numero pubblicato che non è quello che
 * incassi. Non virgola mobile: 0,1 + 0,2 fa 0,30000000000000004, e su denaro
 * vero quell'errore si accumula finché qualcuno spende un millesimo che non ha.
 *
 * Qui non c'è nessun saldo. Il saldo vive in Postgres e si muove solo con le
 * due funzioni SQL del Task 2: un saldo in JavaScript sarebbe un saldo che si
 * riscrive dalla console.
 */

/** Il margine dichiarato, applicato al costo del fornitore. */
export const MARGIN = 0.14;

/** Euro → millesimi interi. */
export const mils = (euro) => Math.round(Number(euro) * 1000);

/** Millesimi → euro, per chi deve scrivere un numero. */
export const toEuro = (m) => m / 1000;

/**
 * Prezzo finale al cliente, dal costo del fornitore. Tutto in millesimi.
 *
 * `Math.round` e non `Math.ceil`: al millesimo, arrotondare per eccesso alza
 * di 0,1 centesimi ogni volta e rigonfia il margine dichiarato. Al più vicino
 * si sbaglia di 0,05 centesimi, e si sbaglia in tutt'e due i versi — che è ciò
 * che rende vera la frase «di ogni euro, 12 centesimi».
 */
export function priceFor(costoMillesimi) {
  const cost = costoMillesimi;
  if (typeof cost !== 'number' || !Number.isInteger(cost) || cost < 0) {
    throw Object.assign(new Error('costo-non-valido'), { code: 'costo-non-valido' });
  }
  const margin = Math.round(cost * MARGIN);
  return { total: cost + margin, cost, margin };
}

/** Il numero che il cliente legge, nella sua lingua. */
export function formatEuro(millesimi, lang = 'it') {
  return new Intl.NumberFormat(lang === 'en' ? 'en-IE' : 'it-IT', {
    style: 'currency',
    currency: 'EUR',
  }).format(toEuro(millesimi));
}
```

⚠️ **Sparisce tutto il resto** — `emptyLedger`, `topUp`, `reserve`, `commit`,
`release`, `purge`, `available`, `HOLD_TTL_MS`. Non si commenta e non si
lascia: è precisamente il codice-mai-collegato che ha prodotto questa
situazione (spec § 1). Il perché sta nella spec § 5.

- [ ] **Step 4: Il listino**

Creare `src/engine/listino.js`:

```js
/**
 * Cosa costa ogni servizio a pagamento, e quanti riferimenti accetta.
 *
 * Un posto solo. I fornitori che arrivano — Seedance per i video, ElevenLabs
 * per le voci — aggiungono **una riga qui** e un adattatore: se aggiungerne
 * uno costasse di più, il giro della generazione avrebbe sbagliato forma
 * (spec § 3.3).
 *
 * I costi sono in **millesimi di euro**, e sono una fotografia: il fornitore
 * incassa in dollari e noi in euro, quindi vanno riguardati. `lavori.costo_reale`
 * dice quando questa riga ha smesso di essere vera.
 */
import { priceFor } from './ledger.js';

export const LISTINO = {
  'immagine-nbp': {
    fornitore: 'google',
    modello: 'gemini-3-pro-image',
    // $0,134 a immagine a 1K–2K sull'API ufficiale (verificato 2026-09-10),
    // a 0,92 €/$ = 123 millesimi. Il 4K costa il doppio e non lo facciamo.
    costo: 123,
    resa: 'png',
    etichetta: 'listino.immagine',
    riferimenti: { personaggio: 5, oggetto: 6, stile: 3, totale: 14 },
  },
};

const NESSUN_RIFERIMENTO = { personaggio: 0, oggetto: 0, stile: 0, totale: 0 };

/** Quanto costa una generazione di questo servizio. */
export function prezzoDi(servizio) {
  const voce = LISTINO[servizio];
  if (!voce) {
    throw Object.assign(new Error(`servizio-sconosciuto: ${servizio}`), {
      code: 'servizio-sconosciuto',
    });
  }
  return priceFor(voce.costo);
}

/**
 * Quanti riferimenti accetta, per ruolo.
 *
 * Un servizio che non li dichiara ne accetta **zero**, invece di lasciar
 * passare immagini a un fornitore che non sa cosa farsene — e che le
 * addebiterebbe lo stesso.
 */
export function limitiDi(servizio) {
  return LISTINO[servizio]?.riferimenti || NESSUN_RIFERIMENTO;
}
```

- [ ] **Step 5: Aggiungere le due chiavi i18n**

In `src/i18n/it.json`, blocco nuovo `listino`: `{ "immagine": "Immagine" }`.
In `src/i18n/en.json`: `{ "immagine": "Image" }`.

- [ ] **Step 6: Far girare — devono passare**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)"
```

- [ ] **Step 7: Commit**

```bash
git add src/engine/ledger.js src/engine/listino.js test/ledger.test.js test/listino.test.js src/i18n
git commit -m "$(cat <<'EOF'
feat: il prezzo si conta in millesimi, e il preventivo E' l'addebito

Il registro esce da `src/store/` ed entra in `src/engine/`, perche' adesso lo
importano in due: il browser per il preventivo, il Worker per l'addebito. Non
sono due tabelle che si assomigliano — e' la stessa funzione, quindi non
possono divergere. E' la § 3.1 della spec di B1 resa impossibile da violare.

MILLESIMI e non centesimi, e la ragione non e' la precisione: e' l'onesta'. Il
margine e' il 14% del costo, cioe' il 12,3% del prezzo; arrotondato al
centesimo su cifre piccole diventa il 14,3%. La home scrive «di ogni euro,
Zack ne rimette 12 centesimi su di se'», e un numero pubblicato che non e'
quello che incassi non e' un arrotondamento, e' una frase falsa. Per lo stesso
motivo `Math.ceil` diventa `Math.round`.

Un test descrive apposta il difetto che il codice adesso NON ha, perche' fra
sei mesi qualcuno proporra' di tornare ai centesimi «che sono piu' semplici».

Via `reserve`, `commit`, `release`, `purge`, `available`, `topUp`: su un server
cio' che impedisce a due schede di spendere lo stesso denaro e' un UPDATE
atomico, non un elenco di prenotazioni in un oggetto JavaScript. E `commit`
addebitava PIU' del preventivo quando il costo reale superava la stima, cioe'
la contraddizione esatta di «il prezzo scritto prima di premere». Cancellato,
non commentato: il codice lasciato li' e' come questo registro e' rimasto
scollegato dal 25 agosto.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Le tabelle, e le due funzioni che rendono impossibile lo scoperto

**Files:** Create `docs/2026-09-10-schema-b2.sql`

**Interfaces:**
- Consumes: la tabella `conti` di B1.
- Produces: le tabelle `lavori` e `movimenti`; le funzioni SQL
  `addebita(uuid, integer) => integer|null` e
  `accredita(uuid, integer, text, text, uuid) => integer`.

- [ ] **Step 1: Scrivere il file SQL**

Creare `docs/2026-09-10-schema-b2.sql`. **Sta nel repository e non solo nel
pannello**: il 2026-09-04 il deploy falliva da giorni perché la configurazione
viveva solo su Cloudflare.

```sql
-- ---------------------------------------------------------------------------
-- B2 — i crediti. Da incollare TUTTO INSIEME nell'editor SQL di Supabase.
--
-- ⚠️ L'editor si ferma alla prima frase che sbaglia e non esegue le successive.
-- E' cosi' che il 2026-09-09 l'`enable row level security` di `conti` e'
-- rimasto indietro, lasciando la tabella aperta a chiunque avesse la chiave
-- pubblica — che sta nel bundle, quindi tutti.
-- ---------------------------------------------------------------------------

-- Il saldo cambia UNITA', non tipo: da centesimi a millesimi di euro.
-- Si puo' fare senza toccare i dati solo perche' vale zero per tutti. Dopo il
-- primo euro incassato, vorrebbe dire moltiplicare per mille dei soldi veri.
comment on column conti.crediti is 'Saldo in MILLESIMI di euro, intero. Mai centesimi, mai virgola mobile.';

-- Cosa e' stato chiesto, e quanto e' costato DAVVERO.
create table if not exists lavori (
  id            uuid primary key,
  utente        uuid not null references auth.users on delete cascade,
  servizio      text not null,
  prezzo        integer not null,          -- millesimi addebitati al cliente
  costo_reale   integer,                   -- millesimi pagati al fornitore
  stato         text not null check (stato in ('in-corso','fatto','fallito','rimborsato')),
  creato_il     timestamptz not null default now(),
  chiuso_il     timestamptz
);
alter table lavori enable row level security;
create index if not exists lavori_appesi on lavori (stato, creato_il);

-- Lo storico. Si scrive e non si cancella: e' la prova di cosa e' successo.
create table if not exists movimenti (
  id            bigserial primary key,
  utente        uuid not null references auth.users on delete cascade,
  millesimi     integer not null,          -- positivo accredita, negativo addebita
  genere        text not null check (genere in ('ricarica','spesa','rimborso')),
  lavoro        uuid references lavori(id),
  -- ⚠️ La riga che impedisce di regalare soldi. Stripe riprova i webhook, e
  -- senza questo vincolo un rinvio accrediterebbe due volte.
  stripe_evento text unique,
  creato_il     timestamptz not null default now()
);
alter table movimenti enable row level security;

-- ---------------------------------------------------------------------------
-- Le due funzioni che muovono il saldo. Nessun altro lo tocca.
--
-- `security definer` le fa girare coi permessi del proprietario, quindi
-- scavalcano la RLS come fa la chiave di servizio. Le chiama solo il Worker.
-- ---------------------------------------------------------------------------

-- ⚠️ E' QUESTA FUNZIONE che rende impossibile lo scoperto, non un controllo
-- in JavaScript prima di chiamare. La guardia sta DENTRO l'update: e' una
-- frase sola, e Postgres non la spezza a meta'. Due schede che generano
-- insieme non possono spendere lo stesso denaro.
create or replace function addebita(p_utente uuid, p_prezzo integer)
returns integer language plpgsql security definer as $$
declare rimasto integer;
begin
  if p_prezzo is null or p_prezzo <= 0 then
    raise exception 'prezzo-non-valido';
  end if;
  update conti set crediti = crediti - p_prezzo
   where utente = p_utente and crediti >= p_prezzo
   returning crediti into rimasto;
  return rimasto;   -- NULL quando il saldo non bastava: niente e' successo
end $$;

-- Accredita e registra il movimento nella STESSA transazione.
-- Separarli vorrebbe dire un saldo che sale senza che niente dica perche', o
-- un movimento che racconta un accredito mai avvenuto.
create or replace function accredita(
  p_utente uuid, p_millesimi integer, p_genere text,
  p_stripe text default null, p_lavoro uuid default null
) returns integer language plpgsql security definer as $$
declare rimasto integer;
begin
  if p_millesimi is null or p_millesimi <= 0 then
    raise exception 'importo-non-valido';
  end if;
  -- Se l'evento Stripe e' gia' passato, il vincolo `unique` fa fallire QUESTA
  -- riga e la transazione intera: il saldo non si muove. E' la difesa contro
  -- il rinvio, e sta prima dell'update apposta.
  insert into movimenti (utente, millesimi, genere, stripe_evento, lavoro)
  values (p_utente, p_millesimi, p_genere, p_stripe, p_lavoro);

  update conti set crediti = crediti + p_millesimi
   where utente = p_utente
   returning crediti into rimasto;
  return rimasto;
end $$;
```

- [ ] **Step 2: Farlo eseguire al committente, e verificarlo**

Il committente incolla il file **tutto insieme** nell'editor SQL di Supabase.
Atteso: *«Success. No rows returned»*.

- [ ] **Step 3: Provare che la RLS è accesa — con un INSERT**

⚠️ **Non con una lettura.** A tabella chiusa e senza policy, `SELECT`, `UPDATE`
e `DELETE` rispondono tutte `200 []` esattamente come a tabella aperta e vuota.
Il 2026-09-10 questo ha prodotto un allarme falso. Solo l'`INSERT` distingue.

```bash
cat > /tmp/prova-rls.mjs <<'EOF'
const SB = 'https://hnbrjihzoxtsnqxeqxtr.supabase.co';
const PUB = 'sb_publishable_x0QPwI7VibEVAG28G1MxGg_FTP6due1';
const FINTO = '00000000-0000-4000-8000-000000000000';
const prove = [
  ['movimenti', { utente: FINTO, millesimi: 100000, genere: 'ricarica' }],
  ['lavori', { id: FINTO, utente: FINTO, servizio: 'x', prezzo: 0, stato: 'fatto' }],
];
for (const [tab, riga] of prove) {
  const r = await fetch(`${SB}/rest/v1/${tab}`, {
    method: 'POST',
    headers: { apikey: PUB, authorization: `Bearer ${PUB}`, 'content-type': 'application/json' },
    body: JSON.stringify(riga),
  });
  const t = await r.text();
  const chiuso = /row-level security/.test(t) || r.status === 403;
  console.log(`${tab.padEnd(11)} ${r.status} ${chiuso ? 'CHIUSA ✅' : 'APERTA 🚨 ' + t.slice(0, 80)}`);
}
EOF
node /tmp/prova-rls.mjs
```

Atteso: **`42501 new row violates row-level security policy`** su tutt'e due.
Se una risponde `201`, quella tabella è aperta al mondo: fermarsi e dirlo.

- [ ] **Step 4: ⚠️ Provare l'atomicità CONTRO POSTGRES, non contro una finzione**

> **Perché questo passo esiste.** Il test di `/genera` (Task 4) finge Supabase
> con una funzione JavaScript che ho scritto io perché si comportasse
> atomicamente. Quel test **non può fallire**: dimostra che la mia finzione è
> atomica, non che lo sia Postgres. È la stessa forma dell'allarme falso sulla
> RLS del 2026-09-10 e del test del muro che passava a vuoto — *una prova che
> dà lo stesso esito nei due mondi non prova niente*.
>
> L'atomicità è l'unica cosa che impedisce a due schede di spendere lo stesso
> denaro. Va provata dove vive.

Dieci chiamate **in parallelo** con credito per tre. La chiave di servizio in
una variabile d'ambiente, mai in un file:

```bash
cat > /tmp/prova-corsa.mjs <<'EOF'
const SB = 'https://hnbrjihzoxtsnqxeqxtr.supabase.co';
const K = process.env.SUPABASE_SERVICE_KEY;
const H = { apikey: K, authorization: `Bearer ${K}`, 'content-type': 'application/json' };
const UTENTE = process.env.UTENTE_DI_PROVA;   // l'uuid del tuo account

const rpc = (n, d) => fetch(`${SB}/rest/v1/rpc/${n}`, { method: 'POST', headers: H, body: JSON.stringify(d) }).then((r) => r.json());
const saldo = async () =>
  (await fetch(`${SB}/rest/v1/conti?utente=eq.${UTENTE}&select=crediti`, { headers: H }).then((r) => r.json()))[0].crediti;

// Credito esatto per TRE addebiti da 140.
const partenza = await saldo();
if (partenza < 420) await rpc('accredita', { p_utente: UTENTE, p_millesimi: 420 - partenza, p_genere: 'ricarica' });

// Dieci in parallelo. Se la guardia non fosse dentro l'UPDATE, ne passerebbero
// piu' di tre e il saldo finirebbe SOTTO ZERO.
const esiti = await Promise.all(
  Array.from({ length: 10 }, () => rpc('addebita', { p_utente: UTENTE, p_prezzo: 140 })),
);
const passati = esiti.filter((e) => e !== null).length;
const finale = await saldo();

console.log(`passati ${passati}/10 · saldo finale ${finale}`);
console.log(passati === 3 && finale === 0
  ? '✅ atomico: la guardia sta dentro l’UPDATE.'
  : `🚨 NON atomico: ne sono passati ${passati} e il saldo e’ ${finale}.`);
EOF
SUPABASE_SERVICE_KEY='...' UTENTE_DI_PROVA='...' node /tmp/prova-corsa.mjs
```

Atteso: **`passati 3/10 · saldo finale 0`**. Un saldo negativo, o più di tre
passati, vuol dire che la guardia non è dentro l'`UPDATE`: fermarsi.

- [ ] **Step 5: Commit**

```bash
git add docs/2026-09-10-schema-b2.sql
git commit -m "$(cat <<'EOF'
feat: le tabelle dei crediti, e le due funzioni che muovono il saldo

Lo schema sta nel REPOSITORY e non solo nel pannello: il 2026-09-04 il deploy
falliva da giorni perche' la configurazione viveva solo su Cloudflare.

⚠️ `addebita()` e' la riga che rende impossibile lo scoperto, e non lo e' un
controllo in JavaScript prima di chiamare. La guardia `crediti >= p_prezzo`
sta DENTRO l'update: e' una frase sola, e Postgres non la spezza a meta'. Due
schede che generano insieme non possono spendere lo stesso denaro. Torna NULL
quando il saldo non basta, e allora non e' successo niente.

`accredita()` inserisce il movimento e alza il saldo nella STESSA transazione,
e l'insert sta PRIMA apposta: se l'evento Stripe e' gia' passato, il vincolo
`unique` fa fallire la riga e la transazione intera, e il saldo non si muove.
E' la difesa contro il rinvio dei webhook — la riga che se manca non rompe
niente e regala soldi.

La RLS si prova con un INSERT anonimo, non con una lettura: a tabella chiusa e
senza policy select, update e delete rispondono `200 []` come a tabella aperta
e vuota. Il 2026-09-10 quella confusione ha prodotto un allarme falso.

E l'atomicita' si prova CONTRO POSTGRES, con dieci chiamate in parallelo e
credito per tre. In Node la si proverebbe contro una finzione scritta da noi
perche' si comportasse atomicamente — un test che non puo' fallire, che e' la
stessa forma dell'allarme falso sulla RLS e del test del muro che passava a
vuoto.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: La ricarica, e la riga che regala soldi

**Files:** Modify `worker/eventi.js`, `worker/index.js`, `test/eventiStripe.test.js`, `test/workerConto.test.js`

**Interfaces:**
- Consumes: da Task 1 — `mils`; da Task 2 — la funzione SQL `accredita`.
- Produces: `PACCHETTI`, `ricaricaDa(evento) => { utente, millesimi, evento } | null` in `worker/eventi.js`; `POST /ricarica` → `{ url }`.

- [ ] **Step 1: I test che falliscono, in `test/eventiStripe.test.js`**

```js
import { LISTINO } from '../src/engine/listino.js';
import { PACCHETTI, ricaricaDa } from '../worker/eventi.js';

test('⚠️ comprare crediti NON deve regalare l’abbonamento', () => {
  /*
   * `checkout.session.completed` scatta per TUTT'E DUE: l'abbonamento e la
   * ricarica. Distinguono solo per `mode`. Senza guardarlo, chiunque compri
   * 5 € di crediti diventerebbe abbonato a vita — e non se ne accorgerebbe
   * nessuno, perche' il cliente e' contento e il difetto lavora a favore suo.
   */
  const ricarica = {
    type: 'checkout.session.completed',
    data: { object: { mode: 'payment', metadata: { utente: 'u-1', millesimi: '5000' } } },
  };
  assert.equal(cosaFare(ricarica), null, 'una ricarica ha attivato un abbonamento');
});

test('la ricarica si riconosce, e porta l’id dell’evento', () => {
  const r = ricaricaDa({
    id: 'evt_1',
    type: 'checkout.session.completed',
    data: { object: { mode: 'payment', metadata: { utente: 'u-1', millesimi: '5000' } } },
  });
  assert.deepEqual(r, { utente: 'u-1', millesimi: 5000, evento: 'evt_1' });
});

test('un abbonamento non e’ una ricarica', () => {
  assert.equal(ricaricaDa({
    id: 'evt_2',
    type: 'checkout.session.completed',
    data: { object: { mode: 'subscription', metadata: { utente: 'u-1' } } },
  }), null);
});

test('una ricarica senza importo, o con un importo storto, non accredita', () => {
  // L'importo arriva dal `metadata` che abbiamo messo noi, ma un evento
  // arriva da fuori: che la firma sia giusta non vuol dire che il corpo lo sia.
  for (const m of [undefined, '', '0', '-100', 'tanti', '99999999999']) {
    const e = {
      id: 'evt_3',
      type: 'checkout.session.completed',
      data: { object: { mode: 'payment', metadata: { utente: 'u-1', millesimi: m } } },
    };
    assert.equal(ricaricaDa(e), null, `«${m}» ha accreditato qualcosa`);
  }
});

test('i tre pacchetti sono quelli decisi, e nessun altro', () => {
  assert.deepEqual(Object.keys(PACCHETTI).sort(), ['p10', 'p25', 'p5']);
  assert.deepEqual(PACCHETTI.p5,  { millesimi: 5000,  centesimi: 500  });
  assert.deepEqual(PACCHETTI.p10, { millesimi: 10000, centesimi: 1000 });
  assert.deepEqual(PACCHETTI.p25, { millesimi: 25000, centesimi: 2500 });
  // Stripe incassa in centesimi, il saldo vive in millesimi: il fattore mille
  // scritto due volte diverge al primo pacchetto nuovo.
  for (const p of Object.values(PACCHETTI)) {
    assert.equal(p.millesimi, p.centesimi * 10);
  }
});

test('un pacchetto vale almeno quanto una generazione', () => {
  // Vendere 5 € di credito quando la generazione piu' cara ne costa di piu'
  // sarebbe vendere qualcosa che non si puo' usare.
  const piuCara = Math.max(...Object.values(LISTINO).map((v) => v.costo));
  assert.ok(PACCHETTI.p5.millesimi > piuCara * 10, 'il pacchetto minimo non compra dieci generazioni');
});
```

E in `test/workerConto.test.js`:

```js
test('/ricarica manda a Stripe il prezzo del LISTINO, non quello del client', async () => {
  /*
   * Un client che dichiara «pacchetto da 25 €» pagandone 5 non deve poter
   * esistere. Il prezzo si prende dall'id, e l'id e' una chiave chiusa.
   */
  const chiamate = rete((url) =>
    url.includes('/auth/v1/user')
      ? new Response(JSON.stringify({ id: 'u-9', email: 'c@e.it' }), { status: 200 })
      : new Response(JSON.stringify({ url: 'https://checkout.stripe.com/x' }), { status: 200 }),
  );

  const res = await worker.fetch(
    new Request('https://zack-app.com/ricarica', {
      method: 'POST',
      headers: { authorization: 'Bearer buono', 'content-type': 'application/json' },
      body: JSON.stringify({ pacchetto: 'p5', centesimi: 1, millesimi: 999999 }),
    }),
    AMBIENTE,
  );
  assert.equal(res.status, 200);

  const aStripe = chiamate.find((c) => c.url.includes('api.stripe.com'));
  const inviato = String(aStripe.corpo);
  assert.match(inviato, /unit_amount=500/, 'il prezzo non viene dal listino dei pacchetti');
  assert.doesNotMatch(inviato, /unit_amount=1\b/, 'ha creduto al prezzo del client');
  assert.match(inviato, /mode=payment/, 'una ricarica non e’ un abbonamento');
  assert.match(inviato, /metadata%5Bmillesimi%5D=5000/);
});

test('un pacchetto inventato non apre nessun pagamento', async () => {
  rete((url) =>
    url.includes('/auth/v1/user')
      ? new Response(JSON.stringify({ id: 'u-9', email: 'c@e.it' }), { status: 200 })
      : null,
  );
  const res = await worker.fetch(
    new Request('https://zack-app.com/ricarica', {
      method: 'POST',
      headers: { authorization: 'Bearer buono', 'content-type': 'application/json' },
      body: JSON.stringify({ pacchetto: 'p1000' }),
    }),
    AMBIENTE,
  );
  assert.equal(res.status, 400);
});

test('lo STESSO evento Stripe non accredita due volte', async () => {
  /*
   * Stripe riprova i webhook, per un timeout o un deploy a meta'. La difesa e'
   * il vincolo `unique` su `movimenti.stripe_evento`: la seconda volta la
   * transazione fallisce, il saldo non si muove, e noi rispondiamo 200 perche'
   * per Stripe e' andata bene — l'aveva gia' fatta.
   */
  const corpo = JSON.stringify({
    id: 'evt_doppio',
    type: 'checkout.session.completed',
    data: { object: { mode: 'payment', metadata: { utente: 'u-1', millesimi: '5000' } } },
  });
  const t = Math.floor(Date.now() / 1000);
  let visto = false;
  rete((url) => {
    if (!url.includes('/rpc/accredita')) return null;
    if (visto) return new Response('{"code":"23505","message":"duplicate key"}', { status: 409 });
    visto = true;
    return new Response('5000', { status: 200 });
  });

  const manda = () =>
    worker.fetch(posta('/webhook', corpo, { 'stripe-signature': firmaPer(corpo, t) }), AMBIENTE);

  assert.equal((await manda()).status, 200);
  assert.equal((await manda()).status, 200, 'il rinvio di Stripe deve ricevere 200, non un errore');
});
```

- [ ] **Step 2: Far girare — devono fallire**

```bash
npm test 2>&1 | grep -E "^not ok" | head
```

- [ ] **Step 3: `worker/eventi.js`**

Aggiungere in cima a `cosaFare`, subito dopo `const oggetto = evento.data?.object || {};`:

```js
  /*
   * ⚠️ Una RICARICA non e' un abbonamento.
   *
   * `checkout.session.completed` scatta per tutt'e due, e distinguono solo per
   * `mode`. Senza questa riga, chiunque compri 5 € di crediti diventerebbe
   * abbonato — e non se ne accorgerebbe nessuno, perche' il cliente e'
   * contento e il difetto lavora a favore suo.
   */
  if (oggetto.mode === 'payment') return null;
```

E in fondo al file:

```js
/**
 * I tre pacchetti. Chiusi: un importo che non e' qui non esiste.
 *
 * Due unita' perche' i due mondi ne usano due: Stripe incassa in **centesimi**,
 * il saldo vive in **millesimi**. Scrivere il fattore mille in due posti vuol
 * dire vederli divergere al primo pacchetto nuovo, e allora qualcuno paga 5 €
 * e ne riceve 50.
 */
export const PACCHETTI = {
  p5: { millesimi: 5000, centesimi: 500 },
  p10: { millesimi: 10000, centesimi: 1000 },
  p25: { millesimi: 25000, centesimi: 2500 },
};

/** Un tetto di sicurezza: nessuna ricarica onesta supera i 100 €. */
const MASSIMO = 100000;

/**
 * Questo evento e' una ricarica? Se si', quanto e per chi.
 *
 * L'importo arriva dal `metadata` che abbiamo messo noi aprendo il pagamento,
 * ma un evento arriva da fuori: che la firma sia giusta non vuol dire che il
 * corpo lo sia. Si legge stretto.
 */
export function ricaricaDa(evento) {
  if (evento?.type !== 'checkout.session.completed') return null;
  const o = evento.data?.object || {};
  if (o.mode !== 'payment') return null;

  const utente = o.metadata?.utente;
  const millesimi = Number(o.metadata?.millesimi);
  if (!utente) return null;
  if (!Number.isInteger(millesimi) || millesimi <= 0 || millesimi > MASSIMO) return null;

  // L'id dell'evento e' cio' che rende il rinvio innocuo (§ 6.2).
  return { utente, millesimi, evento: evento.id };
}
```

- [ ] **Step 4: `/ricarica` in `worker/index.js`**

```js
/**
 * Apre il pagamento di un pacchetto di crediti.
 *
 * `price_data` invece di un Price su Stripe: tre pacchetti sono tre righe qui,
 * non tre prodotti da creare a mano nel pannello e da tenere allineati.
 */
async function ricarica(req, env) {
  const chi = await chiEsegue(req, env);
  if (!chi) return json({ errore: 'non-collegato' }, 401);
  if (!env.STRIPE_SECRET_KEY) return json({ errore: 'non-configurato' }, 503);

  const { pacchetto } = await req.json().catch(() => ({}));
  const scelto = PACCHETTI[pacchetto];
  // ⚠️ Il prezzo viene dall'ID, non dal corpo. Un client che dichiara «25 €»
  // pagandone 5 non deve poter esistere.
  if (!scelto) return json({ errore: 'pacchetto-sconosciuto' }, 400);

  const sito = new URL(req.url).origin;
  const corpo = new URLSearchParams({
    mode: 'payment',
    'line_items[0][price_data][currency]': 'eur',
    'line_items[0][price_data][unit_amount]': String(scelto.centesimi),
    'line_items[0][price_data][product_data][name]': `Crediti Zack — ${scelto.centesimi / 100} €`,
    'line_items[0][quantity]': '1',
    customer_email: chi.email,
    'metadata[utente]': chi.id,
    'metadata[millesimi]': String(scelto.millesimi),
    success_url: `${sito}/app/?ricaricato=1`,
    cancel_url: `${sito}/app/`,
  });

  const res = await fetch('https://api.stripe.com/v1/checkout/sessions', {
    method: 'POST',
    headers: {
      authorization: `Bearer ${env.STRIPE_SECRET_KEY}`,
      'content-type': 'application/x-www-form-urlencoded',
    },
    body: corpo,
  });
  if (!res.ok) return json({ errore: 'stripe' }, 502);
  const s = await res.json();
  return json({ url: s.url });
}
```

Nel `fetch`, accanto alle altre rotte:

```js
    if (url.pathname === '/ricarica' && req.method === 'POST') return ricarica(req, env);
```

L'import in cima, accanto a quello che c'è già:

```js
import { cosaFare, ricaricaDa, PACCHETTI } from './eventi.js';
```

E in `webhookStripe`, **prima** di `cosaFare`:

```js
  const soldi = ricaricaDa(evento);
  if (soldi) {
    const res = await fetch(`${SUPABASE_URL}/rest/v1/rpc/accredita`, {
      method: 'POST',
      headers: conServizio(env),
      body: JSON.stringify({
        p_utente: soldi.utente,
        p_millesimi: soldi.millesimi,
        p_genere: 'ricarica',
        p_stripe: soldi.evento,
      }),
    });
    /*
     * Il duplicato NON e' un errore: e' il rinvio di Stripe che incontra il
     * vincolo `unique`, cioe' la difesa che funziona. A Stripe si risponde
     * 200, perche' per lui era gia' andata bene.
     */
    if (!res.ok && res.status !== 409) return json({ errore: 'archivio' }, 500);
    return json({ ok: true });
  }
```

- [ ] **Step 5: Far girare — devono passare**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)"
```

- [ ] **Step 6: Provare che i due test nuovi mordono**

Togliere la riga `if (oggetto.mode === 'payment') return null;` e rilanciare:
deve fallire «una ricarica ha attivato un abbonamento». Rimetterla.

- [ ] **Step 7: Commit**

```bash
git add worker/eventi.js worker/index.js test/eventiStripe.test.js test/workerConto.test.js
git commit -m "$(cat <<'EOF'
feat: i tre pacchetti di crediti, e il rinvio di Stripe che non regala soldi

⚠️ COMPRARE CREDITI NON DEVE REGALARE L'ABBONAMENTO.
`checkout.session.completed` scatta per tutt'e due e distinguono solo per
`mode`. Senza guardarlo, chiunque comprasse 5 € di crediti diventerebbe
abbonato — e non se ne accorgerebbe nessuno, perche' il cliente e' contento e
il difetto lavora a favore suo. Provato togliendo la riga: il test fallisce.

⚠️ E LO STESSO EVENTO NON ACCREDITA DUE VOLTE. Stripe riprova i webhook, per un
timeout o un deploy a meta'. La difesa e' il vincolo `unique` su
`movimenti.stripe_evento` dentro `accredita()`: la seconda volta la transazione
fallisce e il saldo non si muove. Il 409 non e' un errore, e' la difesa che
funziona — a Stripe si risponde 200, perche' per lui era gia' andata bene.

Il prezzo viene dall'ID del pacchetto, mai dal corpo della richiesta: un client
che dichiara «25 €» pagandone 5 non deve poter esistere.

`price_data` invece di tre Price su Stripe: tre pacchetti sono tre righe nel
codice, non tre prodotti da creare a mano e da tenere allineati.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `/genera` — addebita, chiama, e se fallisce restituisce

**Files:** Create `worker/conto.js`, `worker/fornitori/google.js`, `test/genera.test.js`; Modify `worker/index.js`

**Interfaces:**
- Consumes: da Task 1 — `prezzoDi`, `limitiDi`, `LISTINO`; da Task 2 — `addebita`, `accredita`.
- Produces: `addebita(utente, prezzo, env)`, `rimborsa(utente, prezzo, lavoro, env)`, `apriLavoro(...)`, `chiudiLavoro(...)` in `worker/conto.js`; `generaConGoogle({ voce, prompt, riferimenti, env })` in `worker/fornitori/google.js`; `POST /genera`.

- [ ] **Step 1: I test che falliscono — `test/genera.test.js`**

```js
import { test, afterEach } from 'node:test';
import assert from 'node:assert/strict';
import worker from '../worker/index.js';

/*
 * Il giro dei soldi, provato in Node prima di spendere un centesimo vero.
 *
 * Le due regole che questo file esiste per difendere:
 *   1. non si va sotto zero, nemmeno con due schede aperte;
 *   2. una generazione fallita non costa niente.
 * La seconda non e' una cortesia: hai incassato per una cosa che non e'
 * successa (spec B1 § 3.2).
 */

const AMBIENTE = {
  SUPABASE_SERVICE_KEY: 'sb_secret_finta',
  STRIPE_SECRET_KEY: 'sk_test_finta',
  GOOGLE_API_KEY: 'goog_finta',
  ASSETS: { fetch: async () => new Response('la home', { status: 200 }) },
};

const fetchVero = globalThis.fetch;
afterEach(() => { globalThis.fetch = fetchVero; });

/**
 * Finge Supabase, Google, e tiene un saldo vero: cosi' i test guardano il
 * SALDO invece che le chiamate, che e' quello che conta davvero.
 */
function mondo({ saldo = 5000, googleOk = true } = {}) {
  const stato = { saldo, chiamate: [], lavori: [] };
  globalThis.fetch = async (u, o = {}) => {
    const url = String(u?.url || u);
    const corpo = o.body ? JSON.parse(o.body) : {};
    stato.chiamate.push(url);

    if (url.includes('/auth/v1/user')) {
      return new Response(JSON.stringify({ id: 'u-1', email: 'c@e.it' }), { status: 200 });
    }
    if (url.includes('/rpc/addebita')) {
      if (stato.saldo < corpo.p_prezzo) return new Response('null', { status: 200 });
      stato.saldo -= corpo.p_prezzo;
      return new Response(String(stato.saldo), { status: 200 });
    }
    if (url.includes('/rpc/accredita')) {
      stato.saldo += corpo.p_millesimi;
      return new Response(String(stato.saldo), { status: 200 });
    }
    if (url.includes('/rest/v1/lavori')) {
      stato.lavori.push(corpo);
      return new Response('{}', { status: 201 });
    }
    if (url.includes('generativelanguage.googleapis.com')) {
      return googleOk
        ? new Response(JSON.stringify({
            candidates: [{ content: { parts: [{ inlineData: { mimeType: 'image/png', data: 'AAAA' } }] } }],
            usageMetadata: { totalTokenCount: 1290 },
          }), { status: 200 })
        : new Response('{"error":{"message":"quota"}}', { status: 429 });
    }
    return new Response('{}', { status: 200 });
  };
  return stato;
}

const chiedi = (corpo) =>
  new Request('https://zack-app.com/genera', {
    method: 'POST',
    headers: { authorization: 'Bearer buono', 'content-type': 'application/json' },
    body: JSON.stringify(corpo),
  });

test('una generazione riuscita costa esattamente il preventivo', async () => {
  const w = mondo({ saldo: 5000 });
  const res = await worker.fetch(chiedi({ servizio: 'immagine-nbp', prompt: 'un cane' }), AMBIENTE);
  assert.equal(res.status, 200);
  const corpo = await res.json();
  assert.equal(corpo.mime, 'image/png');
  assert.equal(corpo.prezzo, 140, 'il prezzo risposto non e’ quello del listino');
  assert.equal(w.saldo, 5000 - 140);
});

test('⚠️ una generazione fallita non costa NIENTE', async () => {
  // Il fornitore risponde 429. Hai incassato per una cosa che non e' successa:
  // i crediti tornano, e c'e' un movimento che lo dice.
  const w = mondo({ saldo: 5000, googleOk: false });
  const res = await worker.fetch(chiedi({ servizio: 'immagine-nbp', prompt: 'un cane' }), AMBIENTE);
  assert.equal(res.status, 502);
  assert.equal(w.saldo, 5000, `il saldo e’ sceso a ${w.saldo}: la generazione fallita ha fatto pagare`);
  assert.ok(w.chiamate.some((c) => c.includes('/rpc/accredita')), 'non ha rimborsato');
});

test('senza saldo non si genera, e non si chiama il fornitore', async () => {
  const w = mondo({ saldo: 100 });   // meno dei 140 che serve
  const res = await worker.fetch(chiedi({ servizio: 'immagine-nbp', prompt: 'un cane' }), AMBIENTE);
  assert.equal(res.status, 402);
  assert.equal(w.saldo, 100);
  assert.ok(
    !w.chiamate.some((c) => c.includes('googleapis')),
    'ha chiamato il fornitore senza poterlo pagare: quello lo addebita a NOI',
  );
});

test('l’addebito viene PRIMA della chiamata al fornitore', async () => {
  // Se si chiamasse prima e si addebitasse dopo, due schede in parallelo
  // farebbero due generazioni con il credito per una.
  const w = mondo({ saldo: 5000 });
  await worker.fetch(chiedi({ servizio: 'immagine-nbp', prompt: 'x' }), AMBIENTE);
  const iAddebito = w.chiamate.findIndex((c) => c.includes('/rpc/addebita'));
  const iGoogle = w.chiamate.findIndex((c) => c.includes('googleapis'));
  assert.ok(iAddebito >= 0 && iAddebito < iGoogle, 'ha chiamato il fornitore prima di addebitare');
});

test('un servizio che non e’ a listino non addebita niente', async () => {
  const w = mondo({ saldo: 5000 });
  const res = await worker.fetch(chiedi({ servizio: 'inventato', prompt: 'x' }), AMBIENTE);
  assert.equal(res.status, 400);
  assert.equal(w.saldo, 5000);
});

test('i riferimenti si contano contro il LISTINO', async () => {
  const w = mondo({ saldo: 5000 });
  const troppi = (n, ruolo) =>
    Array.from({ length: n }, () => ({ ruolo, immagine: 'data:image/png;base64,AAAA' }));

  for (const [rif, perche] of [
    [troppi(15, 'oggetto'), 'quindici in tutto'],
    [troppi(6, 'personaggio'), 'sei personaggi quando il massimo e’ cinque'],
    [troppi(4, 'stile'), 'quattro stili quando il massimo e’ tre'],
    [[{ ruolo: 'inventato', immagine: 'data:image/png;base64,AAAA' }], 'un ruolo che non esiste'],
  ]) {
    const res = await worker.fetch(
      chiedi({ servizio: 'immagine-nbp', prompt: 'x', riferimenti: rif }), AMBIENTE);
    assert.equal(res.status, 400, perche);
  }
  assert.equal(w.saldo, 5000, 'ha addebitato una richiesta che il fornitore avrebbe rifiutato');
});

test('il costo REALE si registra', async () => {
  // Senza quel numero, «di ogni euro Zack ne rimette 12 centesimi» e' una
  // promessa che non puoi provare — e una promessa che non puoi provare fa
  // mettere in dubbio anche quelle vere.
  const w = mondo({ saldo: 5000 });
  await worker.fetch(chiedi({ servizio: 'immagine-nbp', prompt: 'x' }), AMBIENTE);
  const chiuso = w.lavori.at(-1);
  assert.equal(typeof chiuso?.costo_reale, 'number');
  assert.equal(chiuso.stato, 'fatto');
});

test('senza token non si genera', async () => {
  const w = mondo();
  const res = await worker.fetch(
    new Request('https://zack-app.com/genera', { method: 'POST', body: '{}' }), AMBIENTE);
  assert.equal(res.status, 401);
  assert.equal(w.saldo, 5000);
});
```

- [ ] **Step 2: Far girare — devono fallire**

```bash
node --test test/genera.test.js 2>&1 | grep -E "^not ok|Cannot find" | head
```

- [ ] **Step 3: `worker/conto.js`**

```js
/**
 * I quattro gesti che muovono il conto. Un posto solo.
 *
 * Sparsi dentro `index.js` diventerebbero quattro varianti leggermente diverse
 * al terzo fornitore, e una di quelle varianti dimenticherebbe il rimborso.
 */
import { SUPABASE_URL } from '../src/lib/supabase.js';

const intestazioni = (env) => ({
  apikey: env.SUPABASE_SERVICE_KEY,
  authorization: `Bearer ${env.SUPABASE_SERVICE_KEY}`,
  'content-type': 'application/json',
});

const rpc = (nome, dati, env) =>
  fetch(`${SUPABASE_URL}/rest/v1/rpc/${nome}`, {
    method: 'POST',
    headers: intestazioni(env),
    body: JSON.stringify(dati),
  });

/**
 * Toglie il prezzo dal saldo. Torna quel che resta, o `null` se non bastava.
 *
 * ⚠️ `null` vuol dire **non è successo niente**: la guardia sta dentro
 * l'`UPDATE` (Task 2), quindi o si addebita o non si tocca il saldo. Non c'è
 * un caso di mezzo, ed è per questo che due schede non possono spendere lo
 * stesso denaro.
 */
export async function addebita(utente, prezzo, env) {
  const res = await rpc('addebita', { p_utente: utente, p_prezzo: prezzo }, env);
  if (!res.ok) return null;
  const rimasto = await res.json();
  return rimasto === null ? null : Number(rimasto);
}

/** Ridà i soldi. È la risposta a ogni fallimento, e non è negoziabile. */
export async function rimborsa(utente, prezzo, lavoro, env) {
  return rpc('accredita', {
    p_utente: utente, p_millesimi: prezzo, p_genere: 'rimborso', p_lavoro: lavoro,
  }, env);
}

/** Apre il lavoro e scrive la spesa: quel che è stato chiesto, e quanto. */
export async function apriLavoro({ id, utente, servizio, prezzo }, env) {
  await fetch(`${SUPABASE_URL}/rest/v1/lavori`, {
    method: 'POST',
    headers: intestazioni(env),
    body: JSON.stringify({ id, utente, servizio, prezzo, stato: 'in-corso' }),
  });
  await fetch(`${SUPABASE_URL}/rest/v1/movimenti`, {
    method: 'POST',
    headers: intestazioni(env),
    body: JSON.stringify({ utente, millesimi: -prezzo, genere: 'spesa', lavoro: id }),
  });
}

/** Chiude il lavoro, col costo vero se c'è. */
export async function chiudiLavoro(id, stato, costoReale, env) {
  await fetch(`${SUPABASE_URL}/rest/v1/lavori?id=eq.${id}`, {
    method: 'PATCH',
    headers: intestazioni(env),
    body: JSON.stringify({
      stato,
      costo_reale: costoReale ?? null,
      chiuso_il: new Date().toISOString(),
    }),
  });
}
```

- [ ] **Step 4: L'adattatore — `worker/fornitori/google.js`**

```js
/**
 * Nano Banana Pro, tradotto.
 *
 * Un adattatore per fornitore, e l'unico posto che sa com'è fatto Google.
 * Seedance ed ElevenLabs ne avranno uno ciascuno e `/genera` non cambierà:
 * è il banco di prova che la spec § 3.3 si è imposta.
 *
 * Dentro entra la stessa cosa per tutti — prompt, riferimenti, la voce di
 * listino — e fuori esce la stessa cosa per tutti.
 */
const BASE = 'https://generativelanguage.googleapis.com/v1beta/models';

/** Un `data:` in ciò che Google si aspetta, o `null` se non è un'immagine. */
function pezzo(riferimento) {
  const m = /^data:(image\/(?:png|jpeg|webp|heic|heif));base64,(.+)$/.exec(riferimento.immagine || '');
  return m ? { inlineData: { mimeType: m[1], data: m[2] } } : null;
}

/**
 * @returns `{ dati, mime, costoReale }`, oppure solleva.
 *   `costoReale` è in millesimi e può essere `null`: meglio non saperlo che
 *   inventarlo — è il numero che prova il margine dichiarato.
 */
export async function generaConGoogle({ voce, prompt, riferimenti = [], env }) {
  const parti = [{ text: prompt }, ...riferimenti.map(pezzo).filter(Boolean)];

  const res = await fetch(`${BASE}/${voce.modello}:generateContent?key=${env.GOOGLE_API_KEY}`, {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({
      contents: [{ parts: parti }],
      // 2K e non 4K: costa il doppio e non lo vendiamo (spec § 6.1). Il
      // «pensiero» basso toglie attesa che su una richiesta con riferimenti
      // non aggiunge niente.
      generationConfig: { imageConfig: { imageSize: '2K' }, thinkingLevel: 'low' },
    }),
  });

  if (!res.ok) {
    throw Object.assign(new Error('fornitore'), { code: 'fornitore', stato: res.status });
  }

  const d = await res.json();
  const inline = d.candidates?.[0]?.content?.parts?.find((p) => p.inlineData)?.inlineData;
  // Una risposta `200` senza immagine è comunque un fallimento: se non si
  // trattasse così, il cliente pagherebbe per un `null`.
  if (!inline?.data) throw Object.assign(new Error('vuoto'), { code: 'fornitore', stato: 200 });

  return { dati: inline.data, mime: inline.mimeType || 'image/png', costoReale: voce.costo };
}
```

- [ ] **Step 5: `/genera` in `worker/index.js`**

```js
/**
 * Genera. **Addebita prima, chiama dopo, e se fallisce restituisce.**
 *
 * L'ordine non è comodità: addebitare dopo la chiamata vorrebbe dire che due
 * schede in parallelo fanno due generazioni col credito per una, e la seconda
 * la paghiamo noi al fornitore.
 */
async function genera(req, env) {
  const chi = await chiEsegue(req, env);
  if (!chi) return json({ errore: 'non-collegato' }, 401);

  const { servizio, prompt, riferimenti = [] } = await req.json().catch(() => ({}));

  const voce = LISTINO[servizio];
  if (!voce) return json({ errore: 'servizio-sconosciuto' }, 400);
  if (typeof prompt !== 'string' || !prompt.trim()) return json({ errore: 'senza-prompt' }, 400);

  // I limiti vengono dal listino, non da costanti scritte qui: il fornitore
  // successivo porta i suoi e questo codice non si tocca.
  const errRif = riferimentiStorti(riferimenti, limitiDi(servizio));
  if (errRif) return json({ errore: errRif }, 400);

  const { total: prezzo } = prezzoDi(servizio);

  const rimasto = await addebita(chi.id, prezzo, env);
  // `null` = il saldo non bastava, e non è successo niente. 402 è lo stato che
  // vuol dire esattamente «servono soldi».
  if (rimasto === null) return json({ errore: 'saldo', prezzo }, 402);

  const lavoro = crypto.randomUUID();
  await apriLavoro({ id: lavoro, utente: chi.id, servizio, prezzo }, env);

  try {
    const { dati, mime, costoReale } = await generaConGoogle({ voce, prompt, riferimenti, env });
    await chiudiLavoro(lavoro, 'fatto', costoReale, env);
    return json({ dati, mime, prezzo, saldo: rimasto, lavoro });
  } catch (e) {
    // Hai incassato per una cosa che non è successa. Non è negoziabile.
    await rimborsa(chi.id, prezzo, lavoro, env);
    await chiudiLavoro(lavoro, 'rimborsato', null, env);
    return json({ errore: 'fornitore', dettaglio: e.code || 'ignoto', saldo: rimasto + prezzo }, 502);
  }
}

/** I riferimenti stanno nei limiti? Il nome del limite rotto, o `null`. */
function riferimentiStorti(riferimenti, limiti) {
  if (!Array.isArray(riferimenti)) return 'riferimenti-storti';
  if (riferimenti.length > limiti.totale) return 'troppi-riferimenti';
  const conta = { personaggio: 0, oggetto: 0, stile: 0 };
  for (const r of riferimenti) {
    if (!(r?.ruolo in conta)) return 'ruolo-sconosciuto';
    conta[r.ruolo] += 1;
  }
  for (const ruolo of Object.keys(conta)) {
    if (conta[ruolo] > limiti[ruolo]) return `troppi-${ruolo}`;
  }
  return null;
}
```

Gli import in cima e la rotta:

```js
import { LISTINO, prezzoDi, limitiDi } from '../src/engine/listino.js';
import { addebita, rimborsa, apriLavoro, chiudiLavoro } from './conto.js';
import { generaConGoogle } from './fornitori/google.js';
```

```js
    if (url.pathname === '/genera' && req.method === 'POST') return genera(req, env);
```

- [ ] **Step 6: Far girare — devono passare, e provare che mordono**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)"
```

Poi togliere la riga `await rimborsa(...)` e rilanciare: deve fallire «una
generazione fallita non costa NIENTE». Rimetterla.

- [ ] **Step 7: Commit**

```bash
git add worker/conto.js worker/fornitori/google.js worker/index.js test/genera.test.js
git commit -m "$(cat <<'EOF'
feat: /genera — addebita, chiama, e se fallisce restituisce

⚠️ L'ORDINE NON E' COMODITA'. Addebitare dopo la chiamata vorrebbe dire che due
schede in parallelo fanno due generazioni col credito per una, e la seconda la
paghiamo noi al fornitore. Un test guarda l'ordine delle chiamate, non solo il
risultato.

⚠️ E UNA GENERAZIONE FALLITA NON COSTA NIENTE. Hai incassato per una cosa che
non e' successa: non e' una cortesia, e' la § 3.2 della spec di B1. Provato
togliendo il rimborso: il test fallisce.

Cio' che rende impossibile lo scoperto e' la funzione SQL, non un controllo
qui: `addebita()` torna null quando il saldo non basta, e allora non e'
successo niente. Nessun caso di mezzo.

Una risposta 200 senza immagine e' comunque un fallimento: senza quella riga il
cliente pagherebbe per un null.

Un adattatore per fornitore, e Google e' l'unico posto che sa com'e' fatto
Google. Seedance ed ElevenLabs ne avranno uno ciascuno e `/genera` non
cambiera': e' il banco di prova che la spec § 3.3 si e' imposta.

`costo_reale` si registra a ogni lavoro. Senza quel numero, «di ogni euro Zack
ne rimette 12 centesimi» e' una promessa che non puoi provare — e una promessa
che non puoi provare fa mettere in dubbio anche quelle vere.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: I lavori appesi, e chi li sblocca

**Files:** Modify `wrangler.jsonc`, `worker/index.js`; Create `test/appesi.test.js`

**Interfaces:**
- Consumes: da Task 2 — `accredita`; da Task 4 — `chiudiLavoro`.
- Produces: `export default { …, scheduled }` in `worker/index.js`.

- [ ] **Step 1: Il test — `test/appesi.test.js`**

```js
import { test, afterEach } from 'node:test';
import assert from 'node:assert/strict';
import worker from '../worker/index.js';

/*
 * Se il Worker muore fra l'addebito e l'esito — il fornitore ci mette troppo,
 * la richiesta scade, il deploy arriva a meta' — l'addebito resta e il rimborso
 * non arriva mai. Non se ne accorge nessuno tranne il cliente, che ha pagato
 * per un'immagine che non ha visto.
 */

const AMBIENTE = { SUPABASE_SERVICE_KEY: 'sb_secret_finta', ASSETS: { fetch: async () => new Response('') } };
const fetchVero = globalThis.fetch;
afterEach(() => { globalThis.fetch = fetchVero; });

test('un lavoro appeso da piu’ di mezz’ora si rimborsa', async () => {
  const vecchio = new Date(Date.now() - 45 * 60000).toISOString();
  const fatte = [];
  globalThis.fetch = async (u, o = {}) => {
    const url = String(u);
    fatte.push({ url, corpo: o.body });
    if (url.includes('/rest/v1/lavori?') && (o.method || 'GET') === 'GET') {
      return new Response(JSON.stringify([
        { id: 'l-1', utente: 'u-1', prezzo: 140, stato: 'in-corso', creato_il: vecchio },
      ]), { status: 200 });
    }
    return new Response('{}', { status: 200 });
  };

  await worker.scheduled({}, AMBIENTE, { waitUntil: () => {} });

  const reso = fatte.find((c) => c.url.includes('/rpc/accredita'));
  assert.ok(reso, 'il lavoro appeso non e’ stato rimborsato');
  assert.match(String(reso.corpo), /"p_millesimi":140/);
  assert.match(String(reso.corpo), /"p_genere":"rimborso"/);
});

test('un lavoro appena partito NON si rimborsa', async () => {
  // Mezz'ora e' molto piu' di qualunque generazione e molto meno della
  // pazienza di chi ha pagato. Rimborsare a due minuti vorrebbe dire
  // rimborsare i video di B3 mentre stanno ancora lavorando.
  const fatte = [];
  globalThis.fetch = async (u, o = {}) => {
    fatte.push(String(u));
    if (String(u).includes('/rest/v1/lavori?') && (o.method || 'GET') === 'GET') {
      // La query filtra per data: se e' scritta bene, non torna niente.
      return new Response('[]', { status: 200 });
    }
    return new Response('{}', { status: 200 });
  };
  await worker.scheduled({}, AMBIENTE, { waitUntil: () => {} });
  assert.ok(!fatte.some((c) => c.includes('/rpc/accredita')), 'ha rimborsato un lavoro vivo');
});
```

- [ ] **Step 2: Far girare — deve fallire** (`worker.scheduled is not a function`)

- [ ] **Step 3: `scheduled` in `worker/index.js`**

```js
/** Oltre questo, un lavoro non sta lavorando: è appeso. */
const APPESO_MINUTI = 30;

/**
 * Ogni ora: i lavori rimasti a metà si rimborsano.
 *
 * Trenta minuti perché è molto più di qualunque generazione — le immagini sono
 * secondi, i video di B3 minuti — e molto meno della pazienza di chi ha pagato.
 *
 * Un lavoro davvero lento che finisce **dopo** il rimborso trova il proprio
 * `lavoro` già chiuso e non riaddebita: meglio regalare una generazione che
 * addebitarne una già restituita.
 */
async function sbloccaAppesi(env) {
  const limite = new Date(Date.now() - APPESO_MINUTI * 60000).toISOString();
  const res = await fetch(
    `${SUPABASE_URL}/rest/v1/lavori?stato=eq.in-corso&creato_il=lt.${limite}&select=*`,
    { headers: conServizio(env) },
  );
  if (!res.ok) return;
  for (const l of await res.json()) {
    await rimborsa(l.utente, l.prezzo, l.id, env);
    await chiudiLavoro(l.id, 'rimborsato', null, env);
  }
}
```

e nell'export:

```js
export default {
  async fetch(req, env) { /* … invariato … */ },
  async scheduled(evento, env) {
    await sbloccaAppesi(env);
  },
};
```

- [ ] **Step 4: Il Cron Trigger in `wrangler.jsonc`**

Accanto a `"routes"`:

```jsonc
  /*
   * Una volta all'ora, al minuto zero. I lavori rimasti a meta' si rimborsano
   * da soli: senza questo, un Worker morto fra l'addebito e l'esito lascia il
   * cliente pagato e senza niente, e non se ne accorge nessuno.
   */
  "triggers": { "crons": ["0 * * * *"] }
```

- [ ] **Step 5: Far girare, costruire, verificare il pacchetto**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)" && npm run build >/dev/null && npx wrangler deploy --dry-run --outdir=/tmp/wdry 2>&1 | grep -E "Total Upload|schedule|✘"
```

- [ ] **Step 6: Commit**

```bash
git add worker/index.js wrangler.jsonc test/appesi.test.js
git commit -m "$(cat <<'EOF'
feat: i lavori rimasti a meta' si rimborsano da soli, ogni ora

Se il Worker muore fra l'addebito e l'esito — il fornitore ci mette troppo, la
richiesta scade, un deploy arriva a meta' — l'addebito resta e il rimborso non
arriva mai. Non se ne accorge nessuno tranne il cliente, che ha pagato per
un'immagine che non ha visto: e quello non torna a dirtelo, torna a non
tornare.

Trenta minuti perche' e' molto piu' di qualunque generazione — le immagini sono
secondi, i video di B3 minuti — e molto meno della pazienza di chi ha pagato.

Un lavoro davvero lento che finisce DOPO il rimborso trova il proprio lavoro
gia' chiuso e non riaddebita: meglio regalare una generazione che addebitarne
una gia' restituita.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Il muro diventa per servizio

**Files:** Modify `src/servizi/index.js`, `src/App.jsx`, `test/servizi.test.js`, `test/muro.test.js`

**Interfaces:**
- Consumes: da B1 — `puoiLavorare(stato)`.
- Produces: `SERVE = ['abbonamento', 'saldo']` e
  `servizioAperto(descrittore, { stato, crediti, prezzo }) => boolean`
  in `src/servizi/index.js`.

> ⚠️ **Perché.** Oggi `chiuso = muroAcceso && !puoiLavorare(statoConto)` chiude
> **tutto** lo studio. Ma il committente ha deciso che i crediti restano a chi
> disdice e si possono spendere: quel muro rende la sua decisione
> irraggiungibile. Chi ha 18 € di credito e nessun abbonamento troverebbe una
> porta chiusa davanti a soldi suoi.

- [ ] **Step 1: I test, in `test/servizi.test.js`**

```js
import { SERVE, servizioAperto, DESCRITTORI } from '../src/servizi/index.js';

test('ogni servizio dichiara COSA gli serve, e da una lista chiusa', () => {
  // Chiusa come `QUANDO` e `LATI`: un valore inventato non chiuderebbe e non
  // aprirebbe, e nessuno saprebbe perche'.
  for (const [id, d] of Object.entries(DESCRITTORI)) {
    assert.ok(SERVE.includes(d.serve), `${id}: «${d.serve}» non e’ nella lista`);
  }
});

test('i cinque strumenti locali chiedono l’abbonamento', () => {
  for (const id of ['scontorna', 'brain', 'vocale', 'effetti', 'vettorializza']) {
    assert.equal(DESCRITTORI[id].serve, 'abbonamento', `${id} ha cambiato regola`);
  }
});

test('chi ha crediti e non ha l’abbonamento GENERA', () => {
  // E' la decisione del committente resa raggiungibile. Senza questo, i suoi
  // soldi sarebbero dietro una porta chiusa.
  const gen = { id: 'immagine', serve: 'saldo' };
  assert.equal(servizioAperto(gen, { stato: 'scaduto', crediti: 5000, prezzo: 140 }), true);
  assert.equal(servizioAperto(gen, { stato: 'mai-entrato', crediti: 5000, prezzo: 140 }), true);
});

test('senza crediti la generazione e’ chiusa anche a chi e’ abbonato', () => {
  // L'abbonamento paga gli strumenti locali, non il fornitore.
  const gen = { id: 'immagine', serve: 'saldo' };
  assert.equal(servizioAperto(gen, { stato: 'aperto', crediti: 100, prezzo: 140 }), false);
  assert.equal(servizioAperto(gen, { stato: 'aperto', crediti: 140, prezzo: 140 }), true);
});

test('gli strumenti locali non si aprono coi crediti', () => {
  // Il verso opposto, e sbagliarlo regalerebbe lo studio a chi carica 5 €.
  const loc = { id: 'scontorna', serve: 'abbonamento' };
  assert.equal(servizioAperto(loc, { stato: 'scaduto', crediti: 99999, prezzo: 0 }), false);
  assert.equal(servizioAperto(loc, { stato: 'prova', crediti: 0, prezzo: 0 }), true);
});
```

E in `test/muro.test.js`:

```js
test('il muro non e’ piu’ uno solo per tutto lo studio', () => {
  /*
   * Il `chiuso` globale chiudeva anche la generazione, e rendeva
   * irraggiungibili i crediti di chi ha disdetto. Questo test chiede che la
   * decisione passi da `servizioAperto`, cioe' dal descrittore.
   */
  assert.match(APP, /servizioAperto\(/, 'App.jsx decide ancora il muro per tutto lo studio');
});
```

- [ ] **Step 2: Far girare — devono fallire**

- [ ] **Step 3: `src/servizi/index.js`**

```js
/**
 * Cosa serve per usare un servizio. **Lista chiusa**, come `QUANDO` e `LATI`.
 *
 * `abbonamento` — gira sul computer del cliente, e l'abbonamento è ciò che lo
 * paga. `saldo` — chiama un fornitore che ci addebita, e a pagarlo sono i
 * crediti: quindi si usa **anche senza abbonamento**, perché quei crediti sono
 * già soldi di chi li ha comprati (decisione del committente, 2026-09-10).
 */
export const SERVE = ['abbonamento', 'saldo'];

/**
 * Questo servizio si può usare adesso?
 *
 * Prende il posto del `chiuso` unico: un muro solo per tutto lo studio
 * chiuderebbe anche la generazione, e chi ha 18 € di credito troverebbe una
 * porta chiusa davanti a soldi suoi.
 */
export function servizioAperto(descrittore, { stato, crediti = 0, prezzo = 0 } = {}) {
  if (descrittore?.serve === 'saldo') return crediti >= prezzo;
  return puoiLavorare(stato);
}
```

con `import { puoiLavorare } from '../engine/licenza.js';` in cima, e dentro
`validaDescrittore`, accanto agli altri controlli:

```js
  if (!SERVE.includes(d.serve)) {
    // Un valore inventato non chiuderebbe e non aprirebbe: lo stesso guasto
    // silenzioso di uno stato fuori da `QUANDO`.
    throw new Error(`Descrittore ${d.id}: «${d.serve}» non è fra ${SERVE.join(', ')}.`);
  }
```

- [ ] **Step 4: `serve: 'abbonamento'` nei cinque descrittori**

In `scontorna.js`, `brain.js`, `vocale.js`, `effetti.js`, `vettorializza.js`,
accanto a `id`:

```js
  /** Gira sul computer del cliente: lo paga l'abbonamento, non i crediti. */
  serve: 'abbonamento',
```

- [ ] **Step 5: `src/App.jsx`**

Sostituire:

```js
  const chiuso = muroAcceso && !puoiLavorare(statoConto);
```

con:

```js
  /**
   * Il muro non è più uno solo.
   *
   * I cinque strumenti locali li paga l'abbonamento; la generazione la pagano
   * i crediti, e quelli restano a chi ha disdetto. Un `chiuso` unico
   * chiuderebbe anche la generazione, cioè metterebbe una porta davanti a
   * soldi che il cliente ha già dato.
   */
  const crediti = licenza?.crediti ?? 0;
  const prezzoQui = DESCRITTORI[tool]?.serve === 'saldo' ? prezzoDi(LISTINO_DI[tool]).total : 0;
  const chiuso =
    muroAcceso &&
    Boolean(DESCRITTORI[tool]) &&
    !servizioAperto(DESCRITTORI[tool], { stato: statoConto, crediti, prezzo: prezzoQui });
```

dove `LISTINO_DI` mappa lo strumento alla voce di listino, dichiarato accanto a
`FACCIA`:

```js
/** Quale voce di listino paga quale strumento. Vuoto per i locali. */
const LISTINO_DI = { immagine: 'immagine-nbp' };
```

e gli import: `import { servizioAperto } from './servizi/index.js';` (accanto
agli altri di quel modulo) e `import { prezzoDi } from './engine/listino.js';`.

- [ ] **Step 6: Far girare, e provare che morde**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)"
```

Cambiare `serve: 'abbonamento'` in `serve: 'saldo'` dentro `scontorna.js`: deve
fallire «i cinque strumenti locali chiedono l'abbonamento». Rimetterlo.

- [ ] **Step 7: Commit**

```bash
git add src/servizi src/App.jsx test/servizi.test.js test/muro.test.js
git commit -m "$(cat <<'EOF'
feat: il muro non e' piu' uno solo — ogni servizio dichiara cosa gli serve

Il committente ha deciso che i crediti restano a chi disdice e si possono
spendere. Il `chiuso` unico rendeva quella decisione irraggiungibile: chi ha
18 € di credito e nessun abbonamento avrebbe trovato una porta chiusa davanti
a soldi suoi.

La chiusura si sposta nel DESCRITTORE, che e' gia' il posto dove ogni servizio
dichiara cosa fa. Nessun meccanismo nuovo: `serve` e' una lista chiusa come
`QUANDO` e `LATI`, e `validaDescrittore` rifiuta un valore inventato — che non
chiuderebbe e non aprirebbe, e nessuno saprebbe perche'.

Due test guardano i due versi, perche' sbagliarne uno costa in due modi
diversi: chi ha crediti e non l'abbonamento DEVE generare, e chi carica 5 € NON
deve ritrovarsi lo studio intero.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Il servizio «Immagine», col prezzo prima del tasto

**Files:** Create `src/servizi/immagine.js`, `src/components/Preventivo.jsx`, `src/components/Riferimenti.jsx`, `test/preventivo.test.js`; Modify `src/servizi/index.js`, `src/App.jsx`, `src/lib/conto.js`, `src/i18n/*.json`, `src/styles.css`

**Interfaces:**
- Consumes: da Task 1 — `prezzoDi`, `limitiDi`, `formatEuro`; da Task 4 — `POST /genera`; dalla libreria — `listAssets()`, `readAsset(id)`.
- Produces: `generaImmagine({ prompt, riferimenti })` in `src/lib/conto.js`; il descrittore `immagine`.

> ⚠️ **Registrare un descrittore È il cablaggio** (`src/servizi/index.js`): si
> registra **con** i suoi gesti, mai prima, o si accende un cerchio che non fa
> niente — il difetto del righello del 2026-09-04.

- [ ] **Step 1: Il test — `test/preventivo.test.js`**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { prezzoDi, limitiDi } from '../src/engine/listino.js';
import { formatEuro } from '../src/engine/ledger.js';
import immagine from '../src/servizi/immagine.js';

const PREVENTIVO = readFileSync(new URL('../src/components/Preventivo.jsx', import.meta.url), 'utf8');

/*
 * «Ogni generazione ti dice quanto costa prima che tu prema.»
 *
 * E' pubblicata sulla home, quindi non e' una preferenza dell'interfaccia: e'
 * una promessa operativa. Questi test la difendono.
 */

test('il preventivo non contiene nessuna cifra scritta a mano', () => {
  /*
   * Una cifra scritta nel componente sarebbe una seconda fonte del prezzo, e
   * il giorno che il listino cambia resterebbe indietro — mostrando un numero
   * e addebitandone un altro. E' la § 3.1: un posto solo.
   */
  assert.doesNotMatch(PREVENTIVO, /\b\d+[.,]\d{2}\s*€/, 'c’e’ un prezzo scritto a mano');
  assert.match(PREVENTIVO, /prezzoDi\(/, 'il preventivo non legge il listino');
  assert.match(PREVENTIVO, /formatEuro\(/);
});

test('il prezzo mostrato e’ quello che il Worker addebita', () => {
  // Le due strade partono dalla stessa funzione: qui si verifica che il numero
  // che finisce sullo schermo sia proprio quello.
  const { total } = prezzoDi('immagine-nbp');
  assert.equal(total, 140);
  assert.equal(formatEuro(total, 'it'), '0,14 €');
});

test('il servizio immagine e’ dichiarato per intero', () => {
  assert.equal(immagine.id, 'immagine');
  assert.equal(immagine.serve, 'saldo');
  assert.equal(immagine.listino, 'immagine-nbp');
  // I limiti non si riscrivono nel descrittore: si chiedono al listino, o
  // divergono al primo fornitore nuovo.
  assert.equal(immagine.riferimenti, undefined, 'il descrittore duplica i limiti del listino');
  assert.ok(limitiDi(immagine.listino).totale > 0);
});
```

- [ ] **Step 2: Far girare — deve fallire**

- [ ] **Step 3: `src/servizi/immagine.js`**

```js
/**
 * «Immagine»: il primo servizio che costa denaro.
 *
 * Il `+` non accetta un file da scontornare — accetta **riferimenti**, cioè
 * asset della libreria che entrano nella generazione. È la promessa che la
 * home fa da settimane e che nessun servizio manteneva: «ritagli un
 * personaggio una volta e diventa un ingrediente».
 *
 * ⚠️ I limiti dei riferimenti NON stanno qui: stanno nel listino, che è
 * l'unico posto che sa cosa accetta il fornitore. Scriverli in due posti vuol
 * dire vederli divergere al primo fornitore nuovo, e allora si addebita una
 * richiesta che il fornitore rifiuta.
 */
export default {
  id: 'immagine',
  claim: 'immagine.claim',

  /** Lo paga il saldo, non l'abbonamento: si usa anche senza abbonarsi. */
  serve: 'saldo',

  /** La voce di listino che dice quanto costa e quanti riferimenti accetta. */
  listino: 'immagine-nbp',

  accetta: { menu: ['personaggio', 'oggetto', 'stile'], quanti: 14 },

  tasto: {
    azione: 'generaImmagine',
    modelli: [],
    fattori: false,
  },

  strumenti: [
    // ⚠️ `icon` dev'essere una chiave VERA di `src/lib/icons.js` e `label` una
    // chiave VERA dell'i18n: un'icona inventata disegna il vuoto, una chiave
    // inventata stampa se stessa sullo schermo. `image` e `stella` esistono in
    // ICONS; le label si creano allo Step 7 di questo stesso task.
    { id: 'riferimenti', icon: 'image', label: 'immagine.riferimenti', quando: 'sempre' },
    { id: 'salva', icon: 'stella', label: 'immagine.salva', quando: 'con-risultato' },
  ],
};
```

Registrarlo in `src/servizi/index.js`:

```js
import immagine from './immagine.js';
export const DESCRITTORI = { scontorna, brain, vocale, effetti, vettorializza, immagine };
```

- [ ] **Step 4: `src/components/Preventivo.jsx`**

```js
import { prezzoDi } from '../engine/listino.js';
import { formatEuro } from '../engine/ledger.js';
import { getLang, t } from '../i18n/index.js';

/**
 * Il prezzo, prima del tasto.
 *
 * *«Ogni generazione ti dice quanto costa prima che tu prema»* è pubblicato
 * sulla home: non è una preferenza dell'interfaccia, è una promessa operativa.
 *
 * ⚠️ **Nessuna cifra qui dentro.** Il numero viene da `prezzoDi`, la stessa
 * funzione che usa il Worker per addebitare. Scriverlo a mano sarebbe una
 * seconda fonte del prezzo, e il giorno che il listino cambia mostreresti un
 * numero addebitandone un altro.
 */
export default function Preventivo({ servizio, saldo }) {
  const { total } = prezzoDi(servizio);
  const lang = getLang();
  const basta = saldo >= total;

  return (
    <p className={`preventivo${basta ? '' : ' preventivo-corto'}`}>
      <strong>{formatEuro(total, lang)}</strong>
      <span>{t('immagine.prezzoNota')}</span>
      {!basta && <span className="preventivo-manca">{t('immagine.saldoCorto')}</span>}
    </p>
  );
}
```

- [ ] **Step 5: `src/components/Riferimenti.jsx`**

```js
import { useEffect, useState } from 'react';
import { limitiDi } from '../engine/listino.js';
import { t } from '../i18n/index.js';

const RUOLI = ['personaggio', 'oggetto', 'stile'];

/**
 * Gli Elements: asset della libreria che entrano nella generazione.
 *
 * Non si caricano ogni volta — **si scelgono dalla libreria**, che è già
 * l'archivio degli asset di chi lavora. Il ruolo si dichiara scegliendo,
 * perché il fornitore tratta un personaggio diversamente da uno stile.
 *
 * ⚠️ **Un riferimento esce dal computer.** È l'unica cosa in tutto il prodotto
 * che lo fa, ed è per forza: generare vuol dire mandare a un fornitore. Si
 * dice qui, accanto al gesto, non in una pagina di aiuto.
 */
export default function Riferimenti({ servizio, scelti, onCambia, assets, onChiudi }) {
  const limiti = limitiDi(servizio);
  const [ruolo, setRuolo] = useState('personaggio');
  const quanti = (r) => scelti.filter((s) => s.ruolo === r).length;
  const pieno = quanti(ruolo) >= limiti[ruolo] || scelti.length >= limiti.totale;

  return (
    <div className="riferimenti">
      <div className="riferimenti-ruoli">
        {RUOLI.map((r) => (
          <button
            key={r}
            className={`btn ghost${r === ruolo ? ' on' : ''}`}
            onClick={() => setRuolo(r)}
          >
            {t(`immagine.ruolo.${r}`)} {quanti(r)}/{limiti[r]}
          </button>
        ))}
      </div>

      <div className="riferimenti-griglia">
        {assets.map((a) => (
          <button
            key={a.id}
            className="brain-chip"
            disabled={pieno}
            title={a.name}
            onClick={() => onCambia([...scelti, { ruolo, assetId: a.id, nome: a.name }])}
          >
            {a.name}
          </button>
        ))}
      </div>

      {scelti.length > 0 && (
        <ul className="riferimenti-scelti">
          {scelti.map((s, i) => (
            <li key={`${s.assetId}-${i}`}>
              {t(`immagine.ruolo.${s.ruolo}`)}: {s.nome}
              <button
                className="btn ghost"
                onClick={() => onCambia(scelti.filter((_, j) => j !== i))}
              >
                {t('immagine.togli')}
              </button>
            </li>
          ))}
        </ul>
      )}

      {/* La riga che dice la verità sul confine, dove il gesto avviene. */}
      <p className="riferimenti-avviso">{t('immagine.escono')}</p>
      <button className="btn" onClick={onChiudi}>{t('immagine.chiudi')}</button>
    </div>
  );
}
```

- [ ] **Step 6: `generaImmagine` in `src/lib/conto.js`**

```js
/**
 * Chiede una generazione al Worker.
 *
 * I riferimenti partono dalla libreria e diventano `data:` **qui**, non nel
 * componente: il componente sa quali asset hai scelto, non come si spediscono.
 */
export async function generaImmagine({ prompt, riferimenti = [], leggiAsset }) {
  const token = await sessione();
  if (!token) throw Object.assign(new Error('non-collegato'), { code: 'non-collegato' });

  const conDati = [];
  for (const r of riferimenti) {
    const dati = await leggiAsset(r.assetId);
    if (dati) conDati.push({ ruolo: r.ruolo, immagine: dati });
  }

  const res = await fetch(`${BASE}/genera`, {
    method: 'POST',
    headers: { authorization: `Bearer ${token}`, 'content-type': 'application/json' },
    body: JSON.stringify({ servizio: 'immagine-nbp', prompt, riferimenti: conDati }),
  });

  const corpo = await res.json().catch(() => ({}));
  if (!res.ok) {
    // Il saldo torna comunque: dopo un fallimento il Worker ha gia' rimborsato,
    // e lo studio deve mostrare il numero giusto senza ricaricare la pagina.
    throw Object.assign(new Error(corpo.errore || 'genera'), {
      code: corpo.errore || 'genera',
      saldo: corpo.saldo,
      prezzo: corpo.prezzo,
    });
  }
  return corpo;   // { dati, mime, prezzo, saldo, lavoro }
}
```

- [ ] **Step 7: Le chiavi i18n**

In `it.json`, blocco `immagine`:

```json
{
  "claim": "Scrivi cosa vuoi vedere. Aggiungi i tuoi asset come riferimento.",
  "riferimenti": "Riferimenti",
  "prezzoNota": "questa generazione, prezzo pieno",
  "saldoCorto": "Non hai abbastanza credito.",
  "escono": "I riferimenti che scegli vengono mandati al fornitore: è l’unica cosa che esce dal tuo computer, e senza mandarla non si può generare.",
  "salva": "Salva in libreria",
  "togli": "Togli",
  "chiudi": "Va bene così",
  "ruolo": { "personaggio": "Personaggio", "oggetto": "Oggetto", "stile": "Stile" }
}
```

In `en.json`:

```json
{
  "claim": "Write what you want to see. Add your own assets as references.",
  "riferimenti": "References",
  "prezzoNota": "this generation, full price",
  "saldoCorto": "You don’t have enough credit.",
  "escono": "The references you pick are sent to the provider: it’s the only thing that leaves your computer, and there’s no generating without sending it.",
  "salva": "Save to library",
  "togli": "Remove",
  "chiudi": "Done",
  "ruolo": { "personaggio": "Character", "oggetto": "Object", "stile": "Style" }
}
```

- [ ] **Step 8: Verificare nel browser**

```bash
npx vite --port 5174 --mode muro
```

Aprire `/app/`, scegliere «Immagine»: il prezzo `0,14 €` si vede **prima** del
tasto Zack; con saldo zero il tasto è chiuso e lo dice; il pannello dei
riferimenti mostra `0/5`, `0/6`, `0/3` e la riga che avverte.

- [ ] **Step 9: Commit**

```bash
git add src/servizi src/components/Preventivo.jsx src/components/Riferimenti.jsx src/lib/conto.js src/App.jsx src/i18n src/styles.css test/preventivo.test.js
git commit -m "$(cat <<'EOF'
feat: «Immagine» — il prezzo prima del tasto, e gli asset come riferimento

«Ogni generazione ti dice quanto costa prima che tu prema» e' pubblicato sulla
home: non e' una preferenza dell'interfaccia, e' una promessa operativa. Un
test rifiuta qualunque cifra scritta a mano dentro il componente — sarebbe una
seconda fonte del prezzo, e il giorno che il listino cambia mostrerebbe un
numero addebitandone un altro.

I riferimenti si SCELGONO DALLA LIBRERIA, non si caricano. E' la promessa che
la home fa da settimane e che nessuno dei sei servizi manteneva: «ritagli un
personaggio una volta e diventa un ingrediente». I tre ruoli — personaggio,
oggetto, stile — sono quelli che Google distingue davvero, e i limiti (5, 6, 3,
14 in tutto) vengono dal LISTINO, non riscritti nel descrittore: due copie
divergono al primo fornitore nuovo, e allora si addebita una richiesta che il
fornitore rifiuta.

⚠️ E si dice, accanto al gesto, che un riferimento ESCE dal computer. E' l'unica
cosa in tutto il prodotto che lo fa, ed e' per forza. Dirlo prima e' la
differenza fra un vantaggio e una bugia scoperta.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Il saldo, e i tre pacchetti

**Files:** Create `src/components/Ricarica.jsx`; Modify `src/App.jsx`, `src/lib/conto.js`, `src/i18n/*.json`, `src/styles.css`, `test/conto.test.js`

**Interfaces:**
- Consumes: da Task 3 — `POST /ricarica`; da Task 1 — `formatEuro`.
- Produces: `vaiAllaRicarica(pacchetto)` in `src/lib/conto.js`.

- [ ] **Step 1: Il test, in `test/conto.test.js`**

```js
test('il saldo che il browser mostra viene dal SERVER, non da se’ stesso', async () => {
  /*
   * Il saldo in `localStorage` e' uno specchio: si riscrive dalla console, e
   * chi lo fa mente a se' stesso perche' il Worker non lo guarda mai. Ma lo
   * studio deve mostrare quello che il server dice, o un utente vede 50 € e
   * riceve un 402 che non capisce.
   */
  globalThis.fetch = async () => risposta({ abbonato: false, crediti: 5000 });
  const l = await chiediLicenza('token-finto');
  assert.equal(l.crediti, 5000);
});

test('i crediti mancanti valgono zero, non «non lo so»', async () => {
  // Un saldo assente non deve diventare `undefined` e poi `NaN` in un
  // confronto: `crediti >= prezzo` con NaN e' sempre falso, e chiuderebbe la
  // generazione a chi ha pagato.
  globalThis.fetch = async () => risposta({ abbonato: true });
  const l = await chiediLicenza('token-finto');
  assert.equal(l.crediti, 0);
  assert.equal(Number.isInteger(l.crediti), true);
});
```

- [ ] **Step 2: `vaiAllaRicarica` in `src/lib/conto.js`**

```js
/**
 * Porta al pagamento di un pacchetto di crediti.
 *
 * Manda **solo l'id** del pacchetto: il prezzo lo decide il Worker. Un browser
 * che dichiara quanto vuole pagare è un browser che paga quanto vuole.
 */
export async function vaiAllaRicarica(pacchetto) {
  const token = await sessione();
  if (!token) throw new Error('non-collegato');
  const res = await fetch(`${BASE}/ricarica`, {
    method: 'POST',
    headers: { authorization: `Bearer ${token}`, 'content-type': 'application/json' },
    body: JSON.stringify({ pacchetto }),
  });
  if (!res.ok) throw new Error('ricarica');
  const { url } = await res.json();
  if (!url) throw new Error('ricarica');
  location.href = url;
}
```

- [ ] **Step 3: `src/components/Ricarica.jsx`**

```js
import { useState } from 'react';
import { formatEuro } from '../engine/ledger.js';
import { getLang, t } from '../i18n/index.js';
import { vaiAllaRicarica } from '../lib/conto.js';

/**
 * I tre pacchetti. Tre tasti e nessuna casella da riempire.
 *
 * Una casella vuota vende meno di tre tasti: chi non sa quanto costa una
 * generazione non sa cosa scriverci. Gli importi sono in millesimi, come tutto
 * il resto del denaro in questo progetto.
 */
const PACCHETTI = [
  { id: 'p5', millesimi: 5000 },
  { id: 'p10', millesimi: 10000 },
  { id: 'p25', millesimi: 25000 },
];

export default function Ricarica({ saldo, onErrore, onChiudi }) {
  const lang = getLang();
  const [inCorso, setInCorso] = useState(null);

  return (
    <div className="ricarica">
      <h3>{t('ricarica.titolo')}</h3>
      <p className="ricarica-saldo">{t('ricarica.hai', { saldo: formatEuro(saldo, lang) })}</p>

      <div className="ricarica-pacchetti">
        {PACCHETTI.map((p) => (
          <button
            key={p.id}
            className="btn"
            disabled={inCorso !== null}
            onClick={async () => {
              setInCorso(p.id);
              try {
                await vaiAllaRicarica(p.id);
              } catch {
                setInCorso(null);
                onErrore(t('ricarica.no'));
              }
            }}
          >
            {formatEuro(p.millesimi, lang)}
          </button>
        ))}
      </div>

      {/* La frase del committente. Il numero viene dal § 6.1 della spec, e un
          test in `test/copyLanding.test.js` fallisce se smette di essere vero. */}
      <p className="ricarica-margine">{t('ricarica.dodici')}</p>
      <p className="ricarica-durata">{t('ricarica.nonScadono')}</p>

      <button className="btn ghost" onClick={onChiudi}>{t('ricarica.chiudi')}</button>
    </div>
  );
}
```

- [ ] **Step 4: Il saldo nella striscia, e il pannello in `App.jsx`**

Accanto all'avviso della prova:

```jsx
      {crediti > 0 && (
        <button className="saldo" onClick={() => setSopraLaTela('ricarica')}>
          {formatEuro(crediti, getLang())}
        </button>
      )}
```

e dentro il gruppo `sopraLaTela`:

```jsx
      {sopraLaTela === 'ricarica' && (
        <Ricarica saldo={crediti} onErrore={setNotice} onChiudi={() => setSopraLaTela(null)} />
      )}
```

- [ ] **Step 5: Le chiavi i18n**

`it.json`, blocco `ricarica`:

```json
{
  "titolo": "Crediti",
  "hai": "Hai {saldo}.",
  "no": "Non sono riuscito ad aprire il pagamento. Riprova fra un momento.",
  "dodici": "Ogni generazione ti dice quanto costa prima che tu prema. E di ogni euro che spendi, Zack ne rimette 12 centesimi su di sé.",
  "nonScadono": "I crediti non scadono, e restano tuoi anche se disdici l’abbonamento.",
  "chiudi": "Va bene così"
}
```

`en.json`:

```json
{
  "titolo": "Credits",
  "hai": "You have {saldo}.",
  "no": "I couldn’t open the payment page. Try again in a moment.",
  "dodici": "Every generation shows you its price before you click. And of every euro you spend, Zack puts 12 cents back into its own brand.",
  "nonScadono": "Credits never expire, and they stay yours even if you cancel your subscription.",
  "chiudi": "Done"
}
```

- [ ] **Step 6: Provare nel browser, e commit**

```bash
git add src/components/Ricarica.jsx src/lib/conto.js src/App.jsx src/i18n src/styles.css test/conto.test.js
git commit -m "$(cat <<'EOF'
feat: i tre pacchetti, il saldo nella striscia, e la frase del committente

Tre tasti e nessuna casella da riempire: chi non sa quanto costa una
generazione non sa cosa scriverci, e una casella vuota vende meno di tre tasti.

Il browser manda SOLO l'id del pacchetto: il prezzo lo decide il Worker. Un
browser che dichiara quanto vuole pagare e' un browser che paga quanto vuole.

Il saldo che si vede viene dal server via `/me`. Quello in `localStorage` e'
uno specchio: si riscrive dalla console, e chi lo fa mente a se' stesso perche'
il Worker non lo guarda mai. Un saldo assente vale ZERO e non `undefined`:
`crediti >= prezzo` con NaN e' sempre falso, e chiuderebbe la generazione a chi
ha pagato.

E la frase, sua: «ogni generazione ti dice quanto costa prima che tu prema; di
ogni euro che spendi, Zack ne rimette 12 centesimi su di se'». Col numero
verificato, non tondo a occhio.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Le tre frasi della home che non sono più vere

**Files:** Modify `src/landing/copy.js`, `test/copyLanding.test.js`

> ⚠️ **Fa parte di B2, non è un lavoro dopo.** È la seconda volta in tre giorni
> che una promessa della home invecchia per qualcosa che costruiamo. La regola:
> **quando una promessa e il codice divergono, si cambia la promessa prima di
> spedire il codice.** Una promessa smentita dal prodotto la trova un cliente,
> non noi.

- [ ] **Step 1: I test che falliscono, in `test/copyLanding.test.js`**

```js
test('la home non promette piu’ che NESSUN file esce', () => {
  /*
   * «I tuoi file non escono da questo computer» e' stata scritta il
   * 2026-09-09 per sostituirne un'altra diventata falsa. Con i riferimenti
   * diventa falsa a meta': un riferimento va a Google, per forza.
   *
   * Resta vera per i sei strumenti locali, quindi si SPACCA IN DUE invece di
   * cancellarla: la parte vera e' il vantaggio piu' grande del prodotto.
   */
  const tutto = JSON.stringify(COPY);
  assert.doesNotMatch(tutto, /non escono da questo computer/, 'promette ancora che niente esce');
  assert.doesNotMatch(tutto, /never leave this computer/);
  assert.match(tutto, /Quando generi|When you generate/i, 'non dice cosa succede generando');
});

test('la home dice che i crediti non scadono', () => {
  // Chi compra 25 € ha diritto di saperlo PRIMA. Ed e' un vantaggio, non una
  // postilla: quasi nessuno lo fa.
  assert.match(JSON.stringify(COPY), /non scadono/);
  assert.match(JSON.stringify(COPY), /never expire/);
});

test('la frase dei dodici centesimi dice il numero vero', () => {
  /*
   * Il 12 non e' una cifra tonda scelta a occhio: e' `margin / total` del
   * listino. Il giorno che il listino cambia e il numero non torna, si cambia
   * la FRASE — non si lascia li'.
   */
  const { margin, total } = prezzoDi('immagine-nbp');
  const centesimiPerEuro = Math.round((margin / total) * 100);
  assert.equal(centesimiPerEuro, 12, `il margine e’ ${centesimiPerEuro} centesimi per euro, non 12`);
  assert.match(JSON.stringify(COPY), /12 centesimi/);
  assert.match(JSON.stringify(COPY), /12 cents/);
});
```

con `import { prezzoDi } from '../src/engine/listino.js';` in cima.

- [ ] **Step 2: Riscrivere `privacy.body` in `src/landing/copy.js`**

Italiano:

```js
      body:
        'Lo scontorno gira nel tuo browser. L’archivio sta nel tuo disco. I sei strumenti non mandano niente da nessuna parte. Quando generi è l’unica volta che qualcosa esce: il riferimento che scegli va al fornitore, perché non si può generare senza mandare niente.',
```

Inglese:

```js
      body:
        "The cutout runs in your browser. The archive sits on your disk. The six tools send nothing anywhere. When you generate is the only time something leaves: the reference you pick goes to the provider, because there's no generating without sending anything.",
```

- [ ] **Step 3: La frase del margine e la durata dei crediti in `generate`**

Italiano:

```js
      example: 'Ogni generazione ti dice quanto costa prima che tu prema.',
      note: 'E di ogni euro che spendi, Zack ne rimette 12 centesimi su di sé. I crediti non scadono e restano tuoi anche se disdici.',
```

Inglese:

```js
      example: 'Every generation shows you its price before you click.',
      note: "And of every euro you spend, Zack puts 12 cents back into its own brand. Credits never expire and stay yours even if you cancel.",
```

- [ ] **Step 4: Far girare, provare che mordono, costruire**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)" && npm run build >/dev/null
```

Rimettere la vecchia frase `i tuoi file non escono da questo computer`: deve
fallire. Rimetterla nuova.

- [ ] **Step 5: Commit**

```bash
git add src/landing/copy.js test/copyLanding.test.js
git commit -m "$(cat <<'EOF'
docs: la home dice cosa esce, cosa no, e dove finiscono i 12 centesimi

Seconda volta in tre giorni che una promessa invecchia per qualcosa che
costruiamo. «I tuoi file non escono da questo computer» era stata scritta il
2026-09-09 per sostituirne un'altra diventata falsa; coi riferimenti diventa
falsa A META', perche' un riferimento va a Google per forza.

Si spacca in due invece di cancellarla: i sei strumenti non mandano niente da
nessuna parte — che e' il vantaggio piu' grande del prodotto e resta intero — e
generare manda il riferimento al fornitore, perche' non si puo' generare senza
mandare niente.

E si dice che i crediti non scadono. Chi compra 25 € ha diritto di saperlo
prima, ed e' un vantaggio, non una postilla: quasi nessuno lo fa.

La frase dei 12 centesimi e' del committente, e il numero e' `margin / total`
del listino, non una cifra tonda scelta a occhio. Un test lo ricalcola: il
giorno che il listino cambia e il numero non torna, fallisce — e allora si
cambia la frase, non si lascia li'.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Al termine

- `npm test && npm run build` verdi;
- **una generazione fallita non costa niente**, e un test lo prova togliendo il rimborso;
- **due schede non spendono lo stesso denaro**, e lo garantisce una frase SQL, non JavaScript;
- **lo stesso evento Stripe non accredita due volte**;
- **comprare crediti non regala l'abbonamento**;
- **il prezzo si vede prima del tasto**, e viene dalla stessa funzione che addebita;
- **chi ha crediti e non ha l'abbonamento genera**; chi carica 5 € non riceve lo studio;
- **la home non promette più niente che il codice smentisca.**

Resta, con la sua spec: **B3** — Seedance per i video, ElevenLabs per audio e
voci. Il banco di prova è già scritto: se aggiungerli costa più di **una riga
di listino e un adattatore**, il giro della generazione ha sbagliato forma e va
raddrizzato prima, non dopo.

E restano le due condizioni per accendere il muro, che vengono da B1 e non da
qui: **il mittente delle email** (oggi è quello condiviso di Supabase, una
manciata di messaggi all'ora) e **il percorso del primo ingresso**, che è
l'unico che non è mai riuscito.
