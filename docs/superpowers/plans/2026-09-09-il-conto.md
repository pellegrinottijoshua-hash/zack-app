# Il conto — account, abbonamento e il muro (B1)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Chi apre lo studio ha un account, paga 3,99 €/mese, e chi non paga trova un muro — senza mai perdere i propri file.

**Architecture:** La **regola** del muro è pura e sta in `src/engine/licenza.js`, dove i test la vedono in Node: è la parte che decide chi lavora e chi no, ed è l'unica che non può sbagliare in silenzio. Intorno le stanno tre cose sciocche: una memoria in `localStorage`, un Worker su Cloudflare che risponde `/me`, e Stripe che dice chi ha pagato.

**Tech Stack:** React 19, `node --test`, Cloudflare Workers, Supabase (auth + Postgres), Stripe Checkout.

**Spec:** [docs/superpowers/specs/2026-09-09-il-conto-design.md](../specs/2026-09-09-il-conto-design.md)

---

## Global Constraints

- **`npm test && npm run build` verdi prima di ogni commit.**
- **`git add <percorsi>`, mai `git add -A`.**
- **Un commit per decisione**, col perché nel corpo.
- **Ogni chiave i18n va in `it.json` e `en.json`**, o un test fallisce.
- **«asset», mai «lavoro/work/piece»** nelle stringhe — e mai `works`, che un test caccia.
- **La libreria non si chiude mai** (spec § 3.5). È la regola che vale su tutte.
- **Nessuna chiave segreta nel browser né in un commit.** Le chiavi stanno in `wrangler secret put`, e i loro nomi in chiaro.
- **Il periodo di grazia è 7 giorni, la prova è 14** (spec § 7.2, § 7.4). Sono costanti nominate, non numeri scritti in mezzo al codice.

---

## Prima di cominciare — quello che deve fare il committente

⚠️ **Tre cose che non posso fare io**, e senza le quali i Task 4-6 non partono. Non servono per i Task 1-3, che si fanno e si provano senza niente.

1. **Supabase**: creare un progetto su supabase.com. Servono `SUPABASE_URL` e `SUPABASE_ANON_KEY` (pubbliche, vanno nel browser) e `SUPABASE_SERVICE_KEY` (**segreta**, solo nel Worker).
2. **Stripe**: creare l'account, un prodotto «Zack App» con un prezzo ricorrente di **3,99 €/mese**, e prendere `STRIPE_SECRET_KEY` e `STRIPE_WEBHOOK_SECRET`.
3. **Il sottodominio**: `api.zack-app.com` puntato al Worker (si fa dal pannello Cloudflare, stesso account del sito).

**Le chiavi segrete non si incollano in chat e non entrano in un file del progetto.** Si mettono con `npx wrangler secret put NOME` dal terminale, che le manda a Cloudflare senza farle passare da nessun'altra parte.

---

## File Structure

| file | responsabilità | task |
|---|---|---|
| **Create** `src/engine/licenza.js` | La regola del muro. Pura: due date, uno stato. | 1 |
| **Create** `test/licenza.test.js` | La tabella dei casi, per intera. | 1 |
| **Create** `src/store/licenza.js` | La memoria della licenza in `localStorage`. | 2 |
| **Create** `test/licenzaMemoria.test.js` | Che ricordi, e che un archivio negato non fermi niente. | 2 |
| **Create** `src/components/Muro.jsx` | Cosa si vede quando non si può lavorare. | 3 |
| **Modify** `src/App.jsx` | Il muro prende il posto del piano; la libreria no. | 3, 7 |
| **Create** `test/muro.test.js` | Che la libreria non sia MAI chiusa. | 3 |
| **Create** `worker/index.js` | `/me`, `/checkout`, `/webhook`. | 4, 6 |
| **Create** `worker/firma.js` | La verifica della firma di Stripe. Pura. | 6 |
| **Create** `test/firmaStripe.test.js` | Che una firma sbagliata non conceda niente. | 6 |
| **Modify** `wrangler.jsonc` | Da Worker-di-soli-asset a Worker con codice. | 4 |
| **Create** `src/lib/conto.js` | Il browser che parla col Worker. | 4, 5 |
| **Modify** `src/landing/copy.js` | La frase «non c'è un server» riscritta. | 8 |

---

## Task 1: La regola del muro

**Files:** Create `src/engine/licenza.js`, `test/licenza.test.js`

**Interfaces:**
- Consumes: niente. È la base.
- Produces: `GRAZIA_GIORNI: 7`, `PROVA_GIORNI: 14`, `statoLicenza(licenza, {adesso}) => 'aperto'|'prova'|'scaduto'|'da-ricollegare'|'mai-entrato'`, `puoiLavorare(stato) => boolean`

- [ ] **Step 1: Scrivere i test che falliscono**

Creare `test/licenza.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { GRAZIA_GIORNI, PROVA_GIORNI, statoLicenza, puoiLavorare } from '../src/engine/licenza.js';

/*
 * Chi puo' lavorare, e chi no.
 *
 * E' la parte che decide se un cliente pagante apre lo studio o resta fuori,
 * quindi e' pura e sta in Node: qui la si guarda tutta in una volta, invece di
 * scoprirla il giorno che qualcuno non riesce a entrare.
 *
 * LE DATE SONO DUE, e confonderle sbaglia in tutt'e due i versi (spec § 7.1):
 *
 *   `validoFino`  fino a quando e' pagato       — lo dice Stripe
 *   `chiestoIl`   quando il server ha risposto  — lo dice il browser
 *
 * Solo la prima: chi disdice e resta offline lavora per un mese intero.
 * Solo la seconda: chi non ha mai pagato lavora per sempre, basta staccare la
 * rete. Servono tutt'e due.
 */

const ORA = new Date('2026-10-01T12:00:00Z');
const giorniFa = (n) => new Date(ORA.getTime() - n * 86400000).toISOString();
const fraGiorni = (n) => new Date(ORA.getTime() + n * 86400000).toISOString();

test('la tabella dei casi, per intera', () => {
  const casi = [
    // [descrizione, licenza, atteso]
    ['nessuna licenza', null, 'mai-entrato'],
    ['abbonato, risposta fresca', { abbonato: true, validoFino: fraGiorni(20), chiestoIl: giorniFa(0) }, 'aperto'],
    ['abbonato, risposta di 6 giorni fa', { abbonato: true, validoFino: fraGiorni(20), chiestoIl: giorniFa(6) }, 'aperto'],
    ['abbonato, risposta di 8 giorni fa', { abbonato: true, validoFino: fraGiorni(20), chiestoIl: giorniFa(8) }, 'da-ricollegare'],
    ['abbonamento scaduto', { abbonato: true, validoFino: giorniFa(1), chiestoIl: giorniFa(0) }, 'scaduto'],
    ['mai abbonato', { abbonato: false, validoFino: null, chiestoIl: giorniFa(0) }, 'scaduto'],
    ['in prova', { abbonato: false, validoFino: null, chiestoIl: giorniFa(0), provaFino: fraGiorni(10) }, 'prova'],
    ['prova finita', { abbonato: false, validoFino: null, chiestoIl: giorniFa(0), provaFino: giorniFa(1) }, 'scaduto'],
  ];
  for (const [che, licenza, atteso] of casi) {
    assert.equal(statoLicenza(licenza, { adesso: ORA }), atteso, `«${che}» dovrebbe dare «${atteso}»`);
  }
});

test('un abbonamento valido NON basta se la risposta e’ vecchia', () => {
  // Il verso che si dimentica: chi ha disdetto ieri ha ancora `validoFino` nel
  // futuro dell'ultima risposta salvata, e senza la seconda data lavorerebbe
  // fino a fine mese senza pagare.
  const l = { abbonato: true, validoFino: fraGiorni(20), chiestoIl: giorniFa(GRAZIA_GIORNI + 1) };
  assert.equal(statoLicenza(l, { adesso: ORA }), 'da-ricollegare');
});

test('una risposta fresca NON basta se l’abbonamento e’ scaduto', () => {
  // L'altro verso: staccare la rete non deve regalare niente.
  const l = { abbonato: true, validoFino: giorniFa(1), chiestoIl: giorniFa(0) };
  assert.equal(statoLicenza(l, { adesso: ORA }), 'scaduto');
});

test('si lavora solo da abbonati o in prova', () => {
  assert.equal(puoiLavorare('aperto'), true);
  assert.equal(puoiLavorare('prova'), true);
  for (const s of ['scaduto', 'da-ricollegare', 'mai-entrato']) {
    assert.equal(puoiLavorare(s), false, `«${s}» non deve far lavorare`);
  }
});

test('uno stato inventato non fa lavorare', () => {
  // Puo' arrivare da una memoria vecchia. Nel dubbio si chiude, perche' il
  // danno di chiudere per sbaglio si ripara con un tasto; quello di aprire
  // per sbaglio no.
  assert.equal(puoiLavorare('boh'), false);
  assert.equal(puoiLavorare(undefined), false);
});

test('una licenza storta non fa lavorare e non esplode', () => {
  for (const rotta of [{}, { abbonato: true }, { chiestoIl: 'ieri' }, { abbonato: true, validoFino: 'mai', chiestoIl: giorniFa(0) }]) {
    const s = statoLicenza(rotta, { adesso: ORA });
    assert.equal(puoiLavorare(s), false, `${JSON.stringify(rotta)} ha fatto passare`);
  }
});

test('i due numeri sono dichiarati, non sparsi nel codice', () => {
  // Si cambieranno. Quando succede si cambiano QUI, e i test lo raccontano.
  assert.equal(GRAZIA_GIORNI, 7);
  assert.equal(PROVA_GIORNI, 14);
});
```

- [ ] **Step 2: Far girare — devono fallire**

```bash
npm test 2>&1 | grep -E "^not ok|Cannot find" | head
```

Atteso: FAIL — `src/engine/licenza.js` non esiste.

- [ ] **Step 3: Scrivere `src/engine/licenza.js`**

```js
/**
 * Chi può lavorare, e chi no.
 *
 * È la parte che decide se un cliente pagante apre lo studio o resta fuori.
 * Pura, e in `engine/` dove i test la vedono in Node: un muro sbagliato non
 * solleva niente — si scopre quando qualcuno che ha pagato non riesce a
 * entrare, cioè nel momento peggiore.
 *
 * **Le date sono DUE** (spec § 7.1), e confonderle sbaglia in tutt'e due i
 * versi:
 *
 * - `validoFino` — fino a quando è pagato l'abbonamento. Lo dice Stripe.
 * - `chiestoIl` — quando il server ha risposto l'ultima volta. Lo dice il
 *   browser.
 *
 * Guardare solo la prima tiene aperto un mese a chi ha disdetto e non è più
 * tornato online. Guardare solo la seconda tiene aperto per sempre a chi non
 * ha mai pagato, purché stacchi la rete.
 */

/**
 * Quanti giorni si lavora senza sentire il server.
 *
 * Gli strumenti girano sul computer del cliente: chiuderlo fuori dai propri
 * strumenti perché un server è giù è il guasto peggiore possibile per questo
 * prodotto. Sette giorni coprono un guasto, un viaggio, una settimana senza
 * rete — e non si possono abusare, perché per allungarli bisogna restare
 * offline, e offline non si genera niente (cioè non si spende niente di chi
 * offre il servizio).
 */
export const GRAZIA_GIORNI = 7;

/** Quanti giorni ha chi usava lo studio quando era gratis (spec § 7.4). */
export const PROVA_GIORNI = 14;

const GIORNO = 86400000;

/** Una data che non si legge non è una data: vale «no», non «adesso». */
function quando(iso) {
  if (typeof iso !== 'string') return null;
  const t = Date.parse(iso);
  return Number.isNaN(t) ? null : t;
}

/**
 * Lo stato della licenza.
 *
 * @returns {'aperto'|'prova'|'scaduto'|'da-ricollegare'|'mai-entrato'}
 */
export function statoLicenza(licenza, { adesso = new Date() } = {}) {
  if (!licenza || typeof licenza !== 'object') return 'mai-entrato';

  const ora = adesso.getTime();
  const chiesto = quando(licenza.chiestoIl);
  if (chiesto === null) return 'mai-entrato';

  // Prima la freschezza: una risposta troppo vecchia non dice più niente su
  // niente, nemmeno sulla prova.
  if (ora - chiesto >= GRAZIA_GIORNI * GIORNO) return 'da-ricollegare';

  const fino = quando(licenza.validoFino);
  if (licenza.abbonato === true && fino !== null && ora < fino) return 'aperto';

  const prova = quando(licenza.provaFino);
  if (prova !== null && ora < prova) return 'prova';

  return 'scaduto';
}

/**
 * Si lavora?
 *
 * Lista chiusa e non «diverso da scaduto»: uno stato che un domani si aggiunge
 * verrebbe fatto passare per distrazione. Il danno di chiudere per sbaglio si
 * ripara con un tasto; quello di aprire per sbaglio no.
 */
export function puoiLavorare(stato) {
  return stato === 'aperto' || stato === 'prova';
}
```

- [ ] **Step 4: Far girare — devono passare**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)"
```

Atteso: PASS, sette test nuovi verdi.

- [ ] **Step 5: Commit**

```bash
npm run build >/dev/null && git add src/engine/licenza.js test/licenza.test.js && git commit -m "$(cat <<'EOF'
feat: la regola del muro, pura e con la tabella dei casi

E' la parte che decide se un cliente pagante apre lo studio o resta fuori,
quindi sta in `engine/` dove i test la vedono in Node. Un muro sbagliato non
solleva niente: si scopre quando qualcuno che ha pagato non riesce a entrare.

LE DATE SONO DUE, e confonderle sbaglia in tutt'e due i versi: solo
`validoFino` tiene aperto un mese a chi ha disdetto e non e' piu' tornato
online; solo `chiestoIl` tiene aperto per sempre a chi non ha mai pagato,
purche' stacchi la rete. Il test le prova separatamente, un verso per volta.

`puoiLavorare` e' una lista chiusa e non «diverso da scaduto»: uno stato
aggiunto un domani verrebbe fatto passare per distrazione, e il danno di
aprire per sbaglio non si ripara con un tasto.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: La memoria della licenza

**Files:** Create `src/store/licenza.js`, `test/licenzaMemoria.test.js`

**Interfaces:**
- Consumes: da Task 1 — niente in codice; la forma `{abbonato, validoFino, chiestoIl, provaFino}`
- Produces: `leggiLicenza(archivio?) => licenza|null`, `salvaLicenza(licenza, archivio?) => boolean`, `CHIAVE: 'jayl.licenza'`

- [ ] **Step 1: Scrivere i test che falliscono**

Creare `test/licenzaMemoria.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { CHIAVE, leggiLicenza, salvaLicenza } from '../src/store/licenza.js';

/*
 * La licenza si ricorda, perche' lo studio deve aprirsi anche senza rete
 * (spec § 3.4). Senza memoria, la grazia di sette giorni non esisterebbe: a
 * ogni apertura si ripartirebbe da «mai entrato».
 */

const finto = () => {
  const dentro = new Map();
  return {
    getItem: (k) => (dentro.has(k) ? dentro.get(k) : null),
    setItem: (k, v) => dentro.set(k, String(v)),
  };
};

test('quello che si salva si rilegge', () => {
  const a = finto();
  const l = { abbonato: true, validoFino: '2026-11-01T00:00:00Z', chiestoIl: '2026-10-01T00:00:00Z' };
  assert.equal(salvaLicenza(l, a), true);
  assert.deepEqual(leggiLicenza(a), l);
});

test('senza niente salvato si risponde «niente», non si esplode', () => {
  assert.equal(leggiLicenza(finto()), null);
});

test('una memoria illeggibile vale «niente»', () => {
  // Puo' essere stata scritta da una versione vecchia, o rovinata a mano. Non
  // deve ne' esplodere ne' far passare: `statoLicenza(null)` dice «mai-entrato».
  const a = finto();
  a.setItem(CHIAVE, '{rotto');
  assert.equal(leggiLicenza(a), null);
});

test('un archivio NEGATO non ferma lo studio', () => {
  /*
   * Finestra anonima, o impostazioni che vietano i dati del sito: `setItem`
   * solleva. Lo studio deve continuare a funzionare per questa sessione —
   * rifiutarsi di partire perche' non si puo' RICORDARE sarebbe peggio del
   * problema che la memoria risolve.
   */
  const negato = {
    getItem: () => {
      throw new Error('negato');
    },
    setItem: () => {
      throw new Error('negato');
    },
  };
  assert.equal(salvaLicenza({ abbonato: true }, negato), false);
  assert.equal(leggiLicenza(negato), null);
});
```

- [ ] **Step 2: Far girare — devono fallire**

```bash
npm test 2>&1 | grep -E "^not ok|Cannot find" | head
```

- [ ] **Step 3: Scrivere `src/store/licenza.js`**

```js
/**
 * La licenza, ricordata.
 *
 * Serve alla grazia dei sette giorni (spec § 3.4): senza memoria si
 * ripartirebbe da «mai entrato» a ogni apertura, e uno studio pagato non si
 * aprirebbe la prima volta che il server ha il raffreddore.
 *
 * `localStorage` e non la Cache API: sono poche centinaia di byte, e devono
 * poter essere letti **prima** di disegnare qualunque cosa.
 */

export const CHIAVE = 'jayl.licenza';

const archivioDiCasa = () => globalThis.localStorage;

/** La licenza salvata, o `null`. Un archivio negato o rovinato vale `null`. */
export function leggiLicenza(archivio = archivioDiCasa()) {
  try {
    const grezzo = archivio?.getItem(CHIAVE);
    if (!grezzo) return null;
    const l = JSON.parse(grezzo);
    return l && typeof l === 'object' ? l : null;
  } catch {
    // Illeggibile o negato: vale «niente». `statoLicenza(null)` dirà
    // «mai-entrato», che è la risposta prudente.
    return null;
  }
}

/** Salva, e dice se c'è riuscita. Un «no» non è un errore: è una sessione sola. */
export function salvaLicenza(licenza, archivio = archivioDiCasa()) {
  try {
    archivio?.setItem(CHIAVE, JSON.stringify(licenza));
    return true;
  } catch {
    return false;
  }
}
```

- [ ] **Step 4: Far girare — devono passare**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)"
```

- [ ] **Step 5: Commit**

```bash
npm run build >/dev/null && git add src/store/licenza.js test/licenzaMemoria.test.js && git commit -m "$(cat <<'EOF'
feat: la licenza si ricorda, e un archivio negato non ferma lo studio

Senza memoria la grazia dei sette giorni non esisterebbe: a ogni apertura si
ripartirebbe da «mai entrato», e uno studio pagato non si aprirebbe la prima
volta che il server ha il raffreddore.

Un archivio negato — finestra anonima, dati del sito vietati — restituisce
`false` invece di sollevare: rifiutarsi di partire perche' non si puo'
RICORDARE sarebbe peggio del problema che la memoria risolve.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Il muro nell'interfaccia, e la libreria che non si chiude

**Files:** Create `src/components/Muro.jsx`, `test/muro.test.js`; Modify `src/App.jsx`, `src/i18n/*.json`, `src/styles.css`

**Interfaces:**
- Consumes: da Task 1 — `statoLicenza`, `puoiLavorare`; da Task 2 — `leggiLicenza`
- Produces: `<Muro stato onEntra onAbbona />` al posto del piano di lavoro

- [ ] **Step 1: Il test che fallisce**

Creare `test/muro.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { puoiLavorare } from '../src/engine/licenza.js';

const APP = readFileSync(new URL('../src/App.jsx', import.meta.url), 'utf8');

/*
 * Il muro chiude gli STRUMENTI. Mai i file di chi li ha fatti.
 *
 * E' la promessa della spec § 3.5, ed e' quella che si rompe per prima
 * mettendo il muro nel posto sbagliato: avvolgere tutta l'app e' la cosa piu'
 * comoda da scrivere, e chiude anche la libreria. Un prodotto che tiene i tuoi
 * file dietro un pagamento non e' un prodotto.
 */

test('la libreria non e’ MAI dietro il muro', () => {
  // Il pannello della libreria e lo «scarica tutto» non stanno dentro nessun
  // ramo che dipende dal poter lavorare: si legge il sorgente, perche' e' una
  // decisione di struttura e non un valore che si possa interrogare.
  const i = APP.indexOf('onDownloadAll');
  assert.notEqual(i, -1, 'lo «scarica tutto» non esiste piu’ in App.jsx');
  const intorno = APP.slice(Math.max(0, i - 2000), i);
  assert.doesNotMatch(
    intorno,
    /puoiLavorare\([^)]*\)\s*(\?|&&)/,
    'la libreria e’ finita dietro il muro: § 3.5 dice che non si chiude mai',
  );
});

test('il muro prende il posto del PIANO, non della schermata', () => {
  assert.match(APP, /<Muro\b/, 'il muro non e’ montato in App.jsx');
  // Se il muro avvolgesse `.shell` chiuderebbe anche la striscia e la libreria.
  const i = APP.indexOf('<Muro');
  const intorno = APP.slice(Math.max(0, i - 600), i);
  assert.doesNotMatch(intorno, /<div className="shell"/, 'il muro avvolge tutta la schermata');
});

test('gli stati che fanno lavorare restano due', () => {
  // Se questo numero cambia e' una decisione, non una cosa che scivola dentro.
  const quanti = ['aperto', 'prova', 'scaduto', 'da-ricollegare', 'mai-entrato'].filter(puoiLavorare);
  assert.deepEqual(quanti, ['aperto', 'prova']);
});
```

- [ ] **Step 2: Far girare — deve fallire**

```bash
npm test 2>&1 | grep -A 3 "il muro prende il posto"
```

Atteso: FAIL — `<Muro` non esiste in App.jsx.

- [ ] **Step 3: Scrivere `src/components/Muro.jsx`**

```jsx
import { t } from '../i18n/index.js';

/**
 * Cosa si vede quando non si può lavorare.
 *
 * Prende il posto del **piano di lavoro**, non della schermata: la striscia in
 * cima e la libreria restano, perché la libreria non si chiude mai (spec
 * § 3.5). Chi non ha pagato deve poter guardare e scaricare i propri file —
 * un prodotto che li tiene in ostaggio non è un prodotto.
 *
 * Quattro stati, quattro frasi diverse. Un muro che dice sempre la stessa cosa
 * manda chi ha un problema di rete a comprare un abbonamento che ha già.
 */
const FRASE = {
  scaduto: 'muro.scaduto',
  'da-ricollegare': 'muro.daRicollegare',
  'mai-entrato': 'muro.maiEntrato',
};

export default function Muro({ stato, onEntra, onAbbona, onGoogle }) {
  // Chi deve solo ricollegarsi non deve vedersi chiedere dei soldi: ce li ha
  // già dati, e chiederglieli di nuovo è il modo più veloce di perderlo.
  const soloEntrare = stato === 'da-ricollegare' || stato === 'mai-entrato';

  return (
    <div className="muro">
      <img
        className="muro-zack"
        src="/zack/zack-disegna.webp"
        alt=""
        aria-hidden="true"
        width="720"
        height="720"
      />
      <h2>{t(`${FRASE[stato] || FRASE['mai-entrato']}.titolo`)}</h2>
      <p className="muro-corpo">{t(`${FRASE[stato] || FRASE['mai-entrato']}.corpo`)}</p>

      <div className="muro-azioni">
        {soloEntrare ? (
          <button className="btn" onClick={onEntra}>
            {t('muro.entra')}
          </button>
        ) : (
          <button className="btn" onClick={onAbbona}>
            {t('muro.abbonati')}
          </button>
        )}
      </div>

      {/* La riga che salva il rapporto: i tuoi file sono lì, e te li porti via
          quando vuoi. Si dice QUI, sul muro, non in una pagina di aiuto. */}
      <p className="muro-libreria">{t('muro.libreriaTua')}</p>
    </div>
  );
}
```

- [ ] **Step 4: Le stringhe, in due lingue**

In `src/i18n/it.json`, blocco `"muro"`:

```json
"muro": {
  "entra": "Entra",
  "abbonati": "Abbonati — 3,99 €/mese",
  "libreriaTua": "I tuoi asset restano tuoi: la libreria si apre e si scarica anche da qui.",
  "scaduto": {
    "titolo": "L'abbonamento non è attivo",
    "corpo": "Gli strumenti dello studio sono chiusi. La tua libreria no: è lì sotto, e puoi scaricarla tutta quando vuoi."
  },
  "daRicollegare": {
    "titolo": "Devi ricollegarti",
    "corpo": "Sono passati più di sette giorni dall'ultima volta che ho potuto controllare. Entra un momento e riparti da dove eri."
  },
  "maiEntrato": {
    "titolo": "Entra per usare lo studio",
    "corpo": "Ti mando un link via email, senza password. Lo scontorno sulla home resta gratis e non chiede niente."
  }
}
```

In `src/i18n/en.json`, lo stesso blocco:

```json
"muro": {
  "entra": "Sign in",
  "abbonati": "Subscribe — €3.99/month",
  "libreriaTua": "Your assets stay yours: the library opens and downloads from here too.",
  "scaduto": {
    "titolo": "Your subscription isn't active",
    "corpo": "The studio tools are closed. Your library isn't: it's right below, and you can download all of it whenever you like."
  },
  "daRicollegare": {
    "titolo": "Please sign in again",
    "corpo": "It's been more than seven days since I could last check. Sign in for a moment and pick up where you left off."
  },
  "maiEntrato": {
    "titolo": "Sign in to use the studio",
    "corpo": "I'll email you a link, no password. The background remover on the home page stays free and asks for nothing."
  }
}
```

- [ ] **Step 5: Montarlo in `src/App.jsx`**

Accanto agli altri `useState`:

```jsx
  /**
   * La licenza, letta dalla memoria all'avvio.
   *
   * Si legge PRIMA di disegnare: il muro non deve comparire un istante dopo lo
   * studio, o chi non ha pagato vede per mezzo secondo la cosa che non può
   * usare — che è peggio di un muro netto.
   */
  const [licenza, setLicenza] = useState(() => leggiLicenza());
  const statoConto = statoLicenza(licenza);
```

E il piano di lavoro, in fondo al `return`, diventa:

```jsx
        <section className="stage">
          {puoiLavorare(statoConto) ? (
            suPiano
          ) : (
            <Muro stato={statoConto} onEntra={entra} onAbbona={abbonati} />
          )}
        </section>
```

⚠️ **Il muro va DENTRO `.stage`**, non intorno a `.shell`: fuori chiuderebbe anche la striscia e la libreria, che è esattamente ciò che § 3.5 vieta e che `test/muro.test.js` controlla.

Per ora `entra` e `abbonati` sono due funzioni che dicono «arriva nel Task 5»:

```jsx
  // Task 5 e 6 le collegano davvero. Un tasto che non fa niente è peggio di un
  // tasto assente: finché non c'è il collegamento, lo dice.
  const entra = () => setNotice(t('muro.presto'));
  const abbonati = () => setNotice(t('muro.presto'));
```

con `muro.presto` = *«Ci sto lavorando: fra poco si potrà entrare da qui.»* / *«Working on it: signing in from here lands soon.»*

- [ ] **Step 6: Il CSS**

In fondo a `src/styles.css`:

```css
/* Il muro: prende il posto del PIANO, non della schermata. La libreria resta
   sotto, e si scarica — spec § 3.5. */
.muro {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 14px;
  min-height: min(60vh, 480px);
  padding: 24px;
  text-align: center;
}
.muro-zack {
  width: 120px;
  height: 120px;
  aspect-ratio: 1;
  object-fit: contain;
}
.muro h2 {
  margin: 0;
  font-family: 'Fredoka', var(--display);
  font-size: clamp(20px, 3.4vw, 27px);
  color: var(--inchiostro);
}
.muro-corpo {
  margin: 0;
  max-width: 46ch;
  line-height: 1.5;
  color: var(--inchiostro-2);
}
.muro-libreria {
  margin: 6px 0 0;
  max-width: 44ch;
  font-size: 13px;
  color: var(--inchiostro-3);
}
```

- [ ] **Step 7: Provare nel browser**

```bash
npx vite
```

Con la memoria vuota lo studio mostra il muro «Entra per usare lo studio», **e la libreria si apre e «scarica tutto» funziona**. Poi, dalla console:

```js
localStorage.setItem('jayl.licenza', JSON.stringify({
  abbonato: true,
  validoFino: new Date(Date.now() + 20*86400000).toISOString(),
  chiestoIl: new Date().toISOString(),
}));
location.reload();
```

Lo studio si apre. Rimettendo `chiestoIl` a otto giorni fa, compare «Devi ricollegarti».

- [ ] **Step 8: Commit**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)" && npm run build >/dev/null
git add src/components/Muro.jsx test/muro.test.js src/App.jsx src/i18n/it.json src/i18n/en.json src/styles.css
git commit -m "$(cat <<'EOF'
feat: il muro chiude gli strumenti, mai la libreria

Il muro prende il posto del PIANO DI LAVORO, non della schermata. Avvolgere
tutta l'app e' la cosa piu' comoda da scrivere e chiude anche la libreria — e
un prodotto che tiene i tuoi file dietro un pagamento non e' un prodotto, e'
un ricatto. Un test legge il sorgente e controlla che lo «scarica tutto» non
finisca mai dentro un ramo che dipende dal poter lavorare.

Quattro stati, quattro frasi. Chi deve solo ricollegarsi non si vede chiedere
dei soldi che ha gia' dato: e' il modo piu' veloce di perderlo.

La licenza si legge PRIMA di disegnare: il muro non deve comparire un istante
dopo lo studio, o chi non ha pagato vede per mezzo secondo la cosa che non
puo' usare.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Il Worker e `/me`

⚠️ Da qui servono le chiavi di **Prima di cominciare**.

**Files:** Create `worker/index.js`, `src/lib/conto.js`; Modify `wrangler.jsonc`, `package.json`

**Interfaces:**
- Consumes: da Task 1 — la forma della licenza
- Produces: `GET /me` → `{email, abbonato, validoFino, provaFino, crediti}`; `chiediLicenza(token) => licenza` in `src/lib/conto.js`

- [ ] **Step 1: `wrangler.jsonc` — da Worker di soli asset a Worker con codice**

Aggiungere `main` accanto ad `assets`:

```jsonc
  /*
   * Da qui il Worker ha del CODICE, non solo file.
   *
   * `assets.directory` resta: le richieste che combaciano con un file statico
   * lo servono come prima, e solo le altre arrivano a `worker/index.js`. Il
   * sito continua a funzionare identico; cambia che `/me`, `/checkout` e
   * `/webhook` ora esistono.
   */
  "main": "worker/index.js",
```

- [ ] **Step 2: Scrivere `worker/index.js`**

```js
/**
 * Il Worker del conto.
 *
 * Sa **chi sei** e **se hai pagato**. Non vede i tuoi file, la tua libreria,
 * le tue tele né le tue registrazioni: quelli non escono dal browser, e questo
 * è un vincolo del prodotto, non una preferenza (spec § 4.1).
 *
 * Sta su Cloudflare perché il sito è già lì: stesso deploy, stessa bolletta,
 * nessun fornitore nuovo per l'infrastruttura.
 */

import { verificaFirma } from './firma.js';
// Lo stesso numero che usa il browser: Wrangler impacchetta tutto insieme,
// quindi non c'e' nessuna ragione di riscriverlo qui.
import { PROVA_GIORNI } from '../src/engine/licenza.js';

const GIORNO = 86400000;

const json = (dati, stato = 200) =>
  new Response(JSON.stringify(dati), {
    status: stato,
    headers: { 'content-type': 'application/json', 'access-control-allow-origin': '*' },
  });

/**
 * Chi sta chiedendo.
 *
 * Il token viene da Supabase e lo verifica Supabase: chiediamo a lui invece di
 * controllare noi una firma JWT. Verificare i token a mano è precisamente il
 * genere di codice che sembra giusto per anni e non lo è.
 */
async function chiEsegue(req, env) {
  const auth = req.headers.get('authorization') || '';
  const token = auth.startsWith('Bearer ') ? auth.slice(7) : null;
  if (!token) return null;

  const res = await fetch(`${env.SUPABASE_URL}/auth/v1/user`, {
    headers: { authorization: `Bearer ${token}`, apikey: env.SUPABASE_ANON_KEY },
  });
  if (!res.ok) return null;
  const u = await res.json();
  return u?.id ? { id: u.id, email: u.email } : null;
}

/** Una riga della tabella `conti`, o `null`. */
async function contoDi(id, env) {
  const res = await fetch(
    `${env.SUPABASE_URL}/rest/v1/conti?utente=eq.${id}&select=*`,
    { headers: { apikey: env.SUPABASE_SERVICE_KEY, authorization: `Bearer ${env.SUPABASE_SERVICE_KEY}` } },
  );
  if (!res.ok) return null;
  const righe = await res.json();
  return righe[0] || null;
}

export default {
  async fetch(req, env) {
    const url = new URL(req.url);

    if (req.method === 'OPTIONS') {
      return new Response(null, {
        headers: {
          'access-control-allow-origin': '*',
          'access-control-allow-headers': 'authorization,content-type',
          'access-control-allow-methods': 'GET,POST,OPTIONS',
        },
      });
    }

    if (url.pathname === '/me') {
      const chi = await chiEsegue(req, env);
      if (!chi) return json({ errore: 'non-collegato' }, 401);
      const conto = await contoDi(chi.id, env);
      return json({
        email: chi.email,
        abbonato: conto?.abbonato === true,
        validoFino: conto?.valido_fino || null,
        // La prova nasce alla prima apparizione: chi usava lo studio quando era
        // gratis non deve trovarlo chiuso da un giorno all'altro (spec § 7.4).
        provaFino: conto?.prova_fino || new Date(Date.now() + PROVA_GIORNI * GIORNO).toISOString(),
        // Il campo esiste da subito e vale zero finché B2 non lo riempie:
        // aggiungerlo dopo vorrebbe dire cambiare una risposta a cui lo studio
        // si è già abituato.
        crediti: conto?.crediti ?? 0,
      });
    }

    if (url.pathname === '/webhook' && req.method === 'POST') {
      return webhookStripe(req, env);
    }

    // Tutto il resto lo servono i file statici, come prima.
    return env.ASSETS.fetch(req);
  },
};

/** Definita nel Task 6. */
async function webhookStripe(req, env) {
  return json({ errore: 'non-ancora' }, 501);
}
```

- [ ] **Step 3: `src/lib/conto.js` — il browser che chiede**

```js
/**
 * Il browser che parla col Worker del conto.
 *
 * Una sola regola, e non è tecnica: **se il server non risponde, non si
 * conclude niente.** La licenza salvata resta valida per la sua grazia (spec
 * § 3.4), e un errore di rete non deve mai diventare «non hai pagato».
 */

const BASE = import.meta.env.VITE_API_BASE || 'https://api.zack-app.com';

/**
 * Chiede al server chi siamo e se abbiamo pagato.
 *
 * @returns la licenza da salvare, oppure `null` se il server non ha risposto —
 *   che NON vuol dire «non abbonato»: vuol dire «non lo so», e chi non lo sa
 *   tiene buona l'ultima risposta.
 */
export async function chiediLicenza(token) {
  try {
    const res = await fetch(`${BASE}/me`, { headers: { authorization: `Bearer ${token}` } });
    if (!res.ok) return null;
    const d = await res.json();
    return {
      abbonato: d.abbonato === true,
      validoFino: d.validoFino,
      provaFino: d.provaFino,
      crediti: d.crediti ?? 0,
      // La seconda data (spec § 7.1): la mette il browser, ora, perché dice
      // «quando ho sentito il server», non «cosa ha detto».
      chiestoIl: new Date().toISOString(),
    };
  } catch {
    return null;
  }
}
```

- [ ] **Step 4: La tabella su Supabase**

Nell'editor SQL di Supabase:

```sql
create table conti (
  utente uuid primary key references auth.users on delete cascade,
  abbonato boolean not null default false,
  valido_fino timestamptz,
  prova_fino timestamptz,
  crediti integer not null default 0,
  stripe_cliente text unique,
  creato_il timestamptz not null default now()
);
-- Nessuno legge questa tabella dal browser: ci parla solo il Worker, con la
-- chiave di servizio. Senza questa riga chiunque potrebbe leggersi i conti
-- altrui con la chiave pubblica.
alter table conti enable row level security;
```

- [ ] **Step 5: Le chiavi, e il deploy**

```bash
npx wrangler secret put SUPABASE_URL
npx wrangler secret put SUPABASE_ANON_KEY
npx wrangler secret put SUPABASE_SERVICE_KEY
npm run build && npx wrangler deploy
```

Poi, dal terminale:

```bash
curl -s https://api.zack-app.com/me | head
```

Atteso: `{"errore":"non-collegato"}` con stato 401 — il Worker risponde, e senza token non concede niente.

- [ ] **Step 6: Commit**

```bash
git add worker/index.js src/lib/conto.js wrangler.jsonc
git commit -m "$(cat <<'EOF'
feat: il Worker del conto, e /me

Da qui il Worker ha del codice e non solo file: `assets.directory` resta, le
richieste che combaciano con un file statico lo servono come prima, e solo le
altre arrivano al codice. Il sito continua identico.

Il server sa CHI SEI e SE HAI PAGATO. Non vede i tuoi file, la tua libreria,
le tue tele ne' le tue registrazioni: e' un vincolo del prodotto, non una
preferenza.

Il token lo verifica Supabase, non noi: controllare a mano la firma di un JWT
e' precisamente il genere di codice che sembra giusto per anni e non lo e'.

`chiediLicenza` restituisce `null` quando il server non risponde, e `null` NON
vuol dire «non abbonato»: vuol dire «non lo so», e chi non lo sa tiene buona
l'ultima risposta salvata. Un errore di rete non deve mai diventare un muro.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: L'ingresso — link magico e Google

**Files:** Modify `src/lib/conto.js`, `src/App.jsx`, `src/components/Muro.jsx`, `src/i18n/*.json`; Modify `package.json`

**Interfaces:**
- Consumes: da Task 4 — `chiediLicenza(token)`
- Produces: `entraConEmail(email)`, `entraConGoogle()`, `sessione()`, `esci()` in `src/lib/conto.js`

- [ ] **Step 1: La dipendenza**

```bash
npm i @supabase/supabase-js
```

- [ ] **Step 2: Aggiungere a `src/lib/conto.js`**

```js
import { createClient } from '@supabase/supabase-js';

/**
 * Il collegamento a Supabase.
 *
 * Le chiavi qui sono PUBBLICHE apposta: `anon` è fatta per stare nel browser,
 * e da sola non legge niente di nessuno — è la RLS sulla tabella `conti` a
 * decidere, e lì nessuno può leggere. La chiave di servizio, quella sì
 * pericolosa, sta solo nel Worker.
 */
const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY,
);

/** Un link via email. Niente password: non averla è meglio che custodirla. */
export async function entraConEmail(email) {
  const { error } = await supabase.auth.signInWithOtp({
    email,
    options: { emailRedirectTo: `${location.origin}/app/` },
  });
  if (error) throw new Error(error.message);
}

export async function entraConGoogle() {
  const { error } = await supabase.auth.signInWithOAuth({
    provider: 'google',
    options: { redirectTo: `${location.origin}/app/` },
  });
  if (error) throw new Error(error.message);
}

/** Il token di adesso, o `null`. */
export async function sessione() {
  const { data } = await supabase.auth.getSession();
  return data?.session?.access_token || null;
}

export async function esci() {
  await supabase.auth.signOut();
}
```

- [ ] **Step 3: Collegare in `src/App.jsx`**

Al posto delle due funzioni finte del Task 3:

```jsx
  /**
   * Chiede al server chi siamo, e si ricorda la risposta.
   *
   * Se il server non risponde NON si tocca la licenza salvata: la grazia dei
   * sette giorni esiste apposta, e sovrascriverla con un fallimento di rete
   * sarebbe chiudere fuori chi ha pagato per colpa di un wifi (spec § 3.4).
   */
  const aggiornaLicenza = useCallback(async () => {
    const token = await sessione();
    if (!token) return;
    const fresca = await chiediLicenza(token);
    if (!fresca) return;
    salvaLicenza(fresca);
    setLicenza(fresca);
  }, []);

  useEffect(() => {
    aggiornaLicenza();
  }, [aggiornaLicenza]);
```

E `entra` apre un campo per l'email dentro il muro; `abbonati` arriva nel Task 6.

- [ ] **Step 4: Il campo dell'email nel muro**

In `src/components/Muro.jsx`, dentro `.muro-azioni` quando `soloEntrare`:

```jsx
        {soloEntrare ? (
          <form
            className="muro-entra"
            onSubmit={(e) => {
              e.preventDefault();
              onEntra(new FormData(e.currentTarget).get('email'));
            }}
          >
            <input
              type="email"
              name="email"
              required
              placeholder={t('muro.email')}
              autoComplete="email"
            />
            <button className="btn" type="submit">
              {t('muro.mandaLink')}
            </button>
            <button className="btn ghost" type="button" onClick={onGoogle}>
              {t('muro.conGoogle')}
            </button>
          </form>
        ) : (
```

Chiavi: `muro.email` = *«la-tua@email.it»*, `muro.mandaLink` = *«Mandami il link»*, `muro.conGoogle` = *«Entra con Google»*, `muro.linkMandato` = *«Ti ho mandato un link. Aprilo da questo dispositivo.»* — e le stesse in inglese.

- [ ] **Step 5: Provare nel browser, e MISURARE `/me`**

Entrare con la propria email, aprire il link, tornare: lo studio deve aprirsi (la prova di 14 giorni è attiva perché `provaFino` arriva dal `/me` del Task 4).

Poi la misura che la spec § 8 chiede, dalla console:

```js
const t0 = performance.now();
await fetch('https://api.zack-app.com/me', { headers: { authorization: `Bearer ${(await window.__sessione?.()) || ''}` } });
console.log('me:', Math.round(performance.now() - t0), 'ms');
```

⚠️ **Il numero va scritto nel commit**, non guardato e dimenticato. Se supera i
**200 ms** va confermato che `/me` sia chiamato **dopo** aver disegnato lo
studio — cioè in `useEffect`, com'è scritto allo Step 3, e non prima del primo
disegno. Un muro che rallenta l'apertura si fa odiare anche da chi ha pagato.

- [ ] **Step 6: Commit**

```bash
npm test 2>&1 | grep -E "^not ok|^# (pass|fail)" && npm run build >/dev/null
git add src/lib/conto.js src/App.jsx src/components/Muro.jsx src/i18n package.json package-lock.json
git commit -m "$(cat <<'EOF'
feat: si entra con un link via email, o con Google. Nessuna password

Una password e' una cosa da custodire, reimpostare, limitare nei tentativi e
non farsi rubare. Non averla toglie tutto quel lavoro e tutta quella
superficie: la scelta migliore e' quella che non c'e'.

Se il server non risponde la licenza salvata NON si tocca: la grazia dei sette
giorni esiste apposta, e sovrascriverla con un fallimento di rete sarebbe
chiudere fuori chi ha pagato per colpa di un wifi.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Stripe — il pagamento, e la firma che si verifica sempre

**Files:** Create `worker/firma.js`, `test/firmaStripe.test.js`; Modify `worker/index.js`, `src/lib/conto.js`, `src/components/Muro.jsx`

**Interfaces:**
- Consumes: da Task 4 — `contoDi`, la tabella `conti`
- Produces: `verificaFirma(corpo, intestazione, segreto, {adesso}) => Promise<boolean>`; `POST /checkout` → `{url}`

- [ ] **Step 1: Il test della firma, che fallisce**

Creare `test/firmaStripe.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createHmac } from 'node:crypto';
import { verificaFirma } from '../worker/firma.js';

/*
 * ⚠️ La riga che, se manca, non rompe niente e REGALA IL PRODOTTO.
 *
 * Un webhook non verificato e' un abbonamento gratis per chiunque sappia fare
 * una POST: basta mandare al nostro `/webhook` un finto «pagamento riuscito».
 * Non solleva errori, non compare nei log come un problema, e si scopre
 * guardando i conti di Stripe che non tornano.
 */

const SEGRETO = 'whsec_prova';
const ORA = new Date('2026-10-01T12:00:00Z');
const firmaPer = (corpo, t) =>
  `t=${t},v1=${createHmac('sha256', SEGRETO).update(`${t}.${corpo}`).digest('hex')}`;

const CORPO = JSON.stringify({ type: 'checkout.session.completed' });
const T = Math.floor(ORA.getTime() / 1000);

test('una firma giusta passa', async () => {
  assert.equal(await verificaFirma(CORPO, firmaPer(CORPO, T), SEGRETO, { adesso: ORA }), true);
});

test('una firma sbagliata NON passa', async () => {
  const falsa = `t=${T},v1=${'0'.repeat(64)}`;
  assert.equal(await verificaFirma(CORPO, falsa, SEGRETO, { adesso: ORA }), false);
});

test('un corpo cambiato dopo la firma NON passa', async () => {
  // E' l'attacco vero: prendere un webhook autentico e cambiarci dentro l'id
  // dell'utente per regalarsi l'abbonamento.
  const firma = firmaPer(CORPO, T);
  const altro = JSON.stringify({ type: 'checkout.session.completed', ladro: true });
  assert.equal(await verificaFirma(altro, firma, SEGRETO, { adesso: ORA }), false);
});

test('una firma vecchia NON passa', async () => {
  // Senza il controllo sull'ora, un webhook autentico intercettato una volta
  // vale per sempre.
  const vecchio = T - 60 * 60;
  assert.equal(await verificaFirma(CORPO, firmaPer(CORPO, vecchio), SEGRETO, { adesso: ORA }), false);
});

test('un’intestazione storta NON passa, e non esplode', async () => {
  for (const brutta of ['', 'boh', 't=,v1=', null, undefined, 't=abc,v1=def']) {
    assert.equal(await verificaFirma(CORPO, brutta, SEGRETO, { adesso: ORA }), false, `«${brutta}»`);
  }
});

test('un segreto vuoto NON fa passare tutto', async () => {
  // Il caso della configurazione dimenticata: senza segreto si deve chiudere,
  // non aprire. E' il difetto peggiore perche' capita in produzione al primo
  // deploy fatto di fretta.
  assert.equal(await verificaFirma(CORPO, firmaPer(CORPO, T), '', { adesso: ORA }), false);
  assert.equal(await verificaFirma(CORPO, firmaPer(CORPO, T), undefined, { adesso: ORA }), false);
});
```

- [ ] **Step 2: Far girare — devono fallire**

```bash
npm test 2>&1 | grep -E "^not ok|Cannot find" | head
```

- [ ] **Step 3: Scrivere `worker/firma.js`**

```js
/**
 * La firma di Stripe, verificata a mano.
 *
 * ⚠️ **È la riga che, se manca, non rompe niente e regala il prodotto.** Un
 * webhook non verificato è un abbonamento gratis per chiunque sappia fare una
 * POST al nostro `/webhook` con dentro un finto «pagamento riuscito». Non
 * solleva errori e non compare nei log come un problema: si scopre guardando i
 * conti di Stripe che non tornano.
 *
 * A mano e non con la libreria di Stripe: sono venti righe di Web Crypto, che
 * gira sia nel Worker sia in Node, quindi **si può provare**. La libreria
 * dentro un Worker si prova molto peggio, e questa è la parte che va provata.
 */

/** Oltre questo scarto un webhook è vecchio: uno intercettato non vale per sempre. */
const TOLLERANZA_SECONDI = 5 * 60;

const esadecimale = (buf) =>
  [...new Uint8Array(buf)].map((b) => b.toString(16).padStart(2, '0')).join('');

/**
 * Confronto a tempo costante.
 *
 * Un `===` su stringhe esce al primo carattere diverso, e la differenza di
 * tempo racconta quanti caratteri erano giusti. Con abbastanza tentativi si
 * indovina una firma un carattere per volta.
 */
function ugualiSenzaFretta(a, b) {
  if (a.length !== b.length) return false;
  let diff = 0;
  for (let i = 0; i < a.length; i++) diff |= a.charCodeAt(i) ^ b.charCodeAt(i);
  return diff === 0;
}

/**
 * La firma è autentica, recente e sul corpo che abbiamo ricevuto?
 *
 * @param {string} corpo il corpo grezzo, esattamente come è arrivato
 * @param {string} intestazione l'header `Stripe-Signature`
 * @param {string} segreto `STRIPE_WEBHOOK_SECRET`
 */
export async function verificaFirma(corpo, intestazione, segreto, { adesso = new Date() } = {}) {
  // Senza segreto si CHIUDE. È il caso della configurazione dimenticata, e
  // aprire sarebbe il difetto peggiore: capita in produzione, al primo deploy
  // fatto di fretta.
  if (!segreto || typeof intestazione !== 'string') return false;

  const parti = Object.fromEntries(
    intestazione.split(',').map((p) => p.split('=').map((x) => x.trim())),
  );
  const t = Number(parti.t);
  const v1 = parti.v1;
  if (!Number.isFinite(t) || !v1) return false;

  if (Math.abs(Math.floor(adesso.getTime() / 1000) - t) > TOLLERANZA_SECONDI) return false;

  const chiave = await crypto.subtle.importKey(
    'raw',
    new TextEncoder().encode(segreto),
    { name: 'HMAC', hash: 'SHA-256' },
    false,
    ['sign'],
  );
  const atteso = esadecimale(
    await crypto.subtle.sign('HMAC', chiave, new TextEncoder().encode(`${t}.${corpo}`)),
  );
  return ugualiSenzaFretta(atteso, v1);
}
```

- [ ] **Step 4: Far girare — devono passare**

```bash
npm test 2>&1 | grep -E "^not ok|^# (tests|pass|fail)"
```

- [ ] **Step 5: `/checkout` e `/webhook` in `worker/index.js`**

Sostituire il segnaposto `webhookStripe` con:

```js
/** Apre il pagamento su Stripe. Il prezzo è già pubblicato: non si inventa qui. */
async function checkout(req, env) {
  const chi = await chiEsegue(req, env);
  if (!chi) return json({ errore: 'non-collegato' }, 401);

  const corpo = new URLSearchParams({
    mode: 'subscription',
    'line_items[0][price]': env.STRIPE_PREZZO,
    'line_items[0][quantity]': '1',
    customer_email: chi.email,
    // Chi ha pagato lo dice Stripe rimandandoci indietro QUESTO: senza,
    // il webhook arriverebbe senza sapere a chi accreditarlo.
    'metadata[utente]': chi.id,
    success_url: `${env.SITO}/app/?pagato=1`,
    cancel_url: `${env.SITO}/app/`,
  });

  const res = await fetch('https://api.stripe.com/v1/checkout/sessions', {
    method: 'POST',
    headers: {
      authorization: `Bearer ${env.STRIPE_SECRET_KEY}`,
      'content-type': 'application/x-www-form-urlencoded',
    },
    body: corpo,
  });
  if (!res.ok) return json({ errore: 'stripe' }, 502);
  const s = await res.json();
  return json({ url: s.url });
}

async function webhookStripe(req, env) {
  // Il corpo GREZZO, prima di qualunque `json()`: la firma è su quei byte, e
  // un `JSON.parse` seguito da `JSON.stringify` li cambia.
  const corpo = await req.text();
  const ok = await verificaFirma(corpo, req.headers.get('stripe-signature'), env.STRIPE_WEBHOOK_SECRET);
  if (!ok) return json({ errore: 'firma' }, 400);

  const evento = JSON.parse(corpo);
  const dati = evento.data?.object || {};
  const utente = dati.metadata?.utente;
  if (!utente) return json({ ok: true });

  const attivo = ['checkout.session.completed', 'invoice.paid'].includes(evento.type);
  const finito = ['customer.subscription.deleted'].includes(evento.type);
  if (!attivo && !finito) return json({ ok: true });

  await fetch(`${env.SUPABASE_URL}/rest/v1/conti`, {
    method: 'POST',
    headers: {
      apikey: env.SUPABASE_SERVICE_KEY,
      authorization: `Bearer ${env.SUPABASE_SERVICE_KEY}`,
      'content-type': 'application/json',
      // Se la riga c'è si aggiorna, se non c'è nasce: il primo pagamento
      // arriva prima che il conto esista.
      prefer: 'resolution=merge-duplicates',
    },
    body: JSON.stringify({
      utente,
      abbonato: attivo,
      valido_fino: attivo
        ? new Date(Date.now() + 32 * GIORNO).toISOString()
        : new Date().toISOString(),
      stripe_cliente: dati.customer || null,
    }),
  });

  return json({ ok: true });
}
```

E nel `fetch`, accanto agli altri percorsi:

```js
    if (url.pathname === '/checkout' && req.method === 'POST') return checkout(req, env);
```

- [ ] **Step 6: Il tasto «Abbonati» collegato**

In `src/lib/conto.js`:

```js
/** Porta a Stripe. Il prezzo lo decide Stripe, non il browser. */
export async function vaiAlPagamento(token) {
  const res = await fetch(`${BASE}/checkout`, {
    method: 'POST',
    headers: { authorization: `Bearer ${token}` },
  });
  if (!res.ok) throw new Error('checkout');
  const { url } = await res.json();
  location.href = url;
}
```

- [ ] **Step 7: Le chiavi, il deploy, e la prova con Stripe di test**

```bash
npx wrangler secret put STRIPE_SECRET_KEY
npx wrangler secret put STRIPE_WEBHOOK_SECRET
npx wrangler secret put STRIPE_PREZZO
npx wrangler secret put SITO
npm run build && npx wrangler deploy
```

Poi, con la CLI di Stripe:

```bash
stripe listen --forward-to https://api.zack-app.com/webhook
stripe trigger checkout.session.completed
```

Atteso: il Worker risponde 200, e la riga in `conti` ha `abbonato = true`.

**E la prova che conta di più:**

```bash
curl -s -X POST https://api.zack-app.com/webhook \
  -H 'stripe-signature: t=1,v1=finta' -d '{"type":"checkout.session.completed"}'
```

Atteso: **400 `{"errore":"firma"}`**. Se risponde 200, il prodotto è gratis per chiunque.

- [ ] **Step 8: Commit**

```bash
npm test 2>&1 | grep -E "^not ok|^# (pass|fail)" && npm run build >/dev/null
git add worker/firma.js worker/index.js test/firmaStripe.test.js src/lib/conto.js src/components/Muro.jsx
git commit -m "$(cat <<'EOF'
feat: Stripe, e la firma del webhook verificata sempre

⚠️ E' la riga che, se manca, non rompe niente e REGALA IL PRODOTTO: un webhook
non verificato e' un abbonamento gratis per chiunque sappia fare una POST col
dentro un finto «pagamento riuscito». Non solleva errori, non compare nei log
come un problema, e si scopre guardando i conti di Stripe che non tornano.

Verificata a mano e non con la libreria di Stripe: sono venti righe di Web
Crypto che girano sia nel Worker sia in Node, quindi SI PUO' PROVARE — e
questa e' precisamente la parte che va provata. Sei test: firma giusta, firma
sbagliata, corpo cambiato dopo la firma, firma vecchia, intestazione storta, e
SEGRETO MANCANTE, che e' il caso della configurazione dimenticata e deve
chiudere invece di aprire.

Il confronto e' a tempo costante: un `===` esce al primo carattere diverso, e
la differenza di tempo racconta quanti erano giusti.

Il corpo si legge GREZZO prima di qualunque `json()`: la firma e' su quei
byte, e un parse seguito da uno stringify li cambia.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: I quattordici giorni a chi c'era già

**Files:** Modify `worker/index.js`, `src/components/Muro.jsx`, `src/i18n/*.json`

**Interfaces:**
- Consumes: da Task 1 — `PROVA_GIORNI`; da Task 4 — `/me`

- [ ] **Step 1: La prova nasce una volta sola**

Nel Task 4 `/me` inventa `provaFino` a ogni chiamata, il che vuol dire **prova infinita**. Va scritta la prima volta e poi riletta:

```js
    if (url.pathname === '/me') {
      const chi = await chiEsegue(req, env);
      if (!chi) return json({ errore: 'non-collegato' }, 401);
      let conto = await contoDi(chi.id, env);

      /*
       * La prova nasce alla PRIMA apparizione e poi si rilegge.
       *
       * Calcolarla a ogni chiamata — «da adesso, quattordici giorni» — dà una
       * prova che non finisce mai: è un difetto che non si vede provando, si
       * vede fra due settimane quando nessuno ha ancora pagato.
       */
      if (!conto) {
        // `PROVA_GIORNI` viene da `src/engine/licenza.js`, non riscritto: due
        // copie dello stesso numero divergono al primo ripensamento, e qui
        // divergere vuol dire che il browser e il server non sono d'accordo su
        // quando finisce la prova di un cliente.
        const prova = new Date(Date.now() + PROVA_GIORNI * GIORNO).toISOString();
        await fetch(`${env.SUPABASE_URL}/rest/v1/conti`, {
          method: 'POST',
          headers: {
            apikey: env.SUPABASE_SERVICE_KEY,
            authorization: `Bearer ${env.SUPABASE_SERVICE_KEY}`,
            'content-type': 'application/json',
          },
          body: JSON.stringify({ utente: chi.id, prova_fino: prova }),
        });
        conto = { prova_fino: prova, abbonato: false, crediti: 0 };
      }

      return json({
        email: chi.email,
        abbonato: conto.abbonato === true,
        validoFino: conto.valido_fino || null,
        provaFino: conto.prova_fino || null,
        crediti: conto.crediti ?? 0,
      });
    }
```

- [ ] **Step 2: Dirlo, invece di lasciarlo scoprire**

In `src/components/Muro.jsx` non serve niente — chi è in prova **lavora**. Ma va detto quanto manca, e va detto nello studio, non sul muro. In `src/App.jsx`, sotto la striscia:

```jsx
        {statoConto === 'prova' && (
          /* Una prova che finisce senza preavviso e' una porta chiusa in
             faccia. Si dice ogni giorno, in una riga, senza fermare niente. */
          <p className="avviso-prova">
            {t('muro.provaResta', { giorni: giorniAllaProva(licenza) })}
          </p>
        )}
```

con `giorniAllaProva` in `src/engine/licenza.js`:

```js
/** Quanti giorni restano di prova. Zero se non è in prova. */
export function giorniAllaProva(licenza, { adesso = new Date() } = {}) {
  const fine = quando(licenza?.provaFino);
  if (fine === null) return 0;
  return Math.max(0, Math.ceil((fine - adesso.getTime()) / GIORNO));
}
```

E il test, in `test/licenza.test.js`:

```js
test('i giorni di prova che restano si contano per eccesso', () => {
  // Per eccesso: «restano 0 giorni» a chi ne ha ancora mezzo e' una bugia che
  // fa chiudere l'app e non riaprirla.
  const fra = (n) => new Date(ORA.getTime() + n * 86400000).toISOString();
  assert.equal(giorniAllaProva({ provaFino: fra(13.2) }, { adesso: ORA }), 14);
  assert.equal(giorniAllaProva({ provaFino: fra(0.3) }, { adesso: ORA }), 1);
  assert.equal(giorniAllaProva({ provaFino: fra(-1) }, { adesso: ORA }), 0);
  assert.equal(giorniAllaProva({}, { adesso: ORA }), 0);
});
```

Chiave `muro.provaResta` = *«Prova: restano {giorni} giorni.»* / *«Trial: {giorni} days left.»*

- [ ] **Step 3: Commit**

```bash
npm test 2>&1 | grep -E "^not ok|^# (pass|fail)" && npm run build >/dev/null
git add worker/index.js src/engine/licenza.js test/licenza.test.js src/App.jsx src/i18n
git commit -m "$(cat <<'EOF'
feat: quattordici giorni a chi usava lo studio quando era gratis

Non e' generosita': chiudere una porta in faccia a chi era entrato quando era
aperta e' il modo piu' veloce di non rivederlo.

La prova nasce alla PRIMA apparizione e poi si rilegge. Calcolarla a ogni
chiamata — «da adesso, quattordici giorni» — da' una prova che non finisce
mai: un difetto che non si vede provando, si vede fra due settimane quando
nessuno ha ancora pagato.

I giorni che restano si contano per ECCESSO: dire «restano 0 giorni» a chi ne
ha ancora mezzo e' una bugia che fa chiudere l'app e non riaprirla.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: La frase sulla home che diventa falsa

**Files:** Modify `src/landing/copy.js`, `test/copyLanding.test.js`

- [ ] **Step 1: Il test che fallisce**

In `test/copyLanding.test.js`:

```js
test('la home non dice piu’ che non c’e’ un server', () => {
  /*
   * Il giorno che B1 va in produzione un server c'e'. La frase «non c'e' un
   * server che li guarda, perche' non c'e' un server» diventa falsa alla
   * lettera, e una promessa smentita dal prodotto la trova un cliente, non noi.
   *
   * Cio' che resta VERO, e che si continua a dire: i file non escono dal
   * computer di chi lavora.
   */
  const tutto = JSON.stringify(COPY);
  assert.doesNotMatch(tutto, /non c.è un server/i, 'la home promette ancora che non c’e’ un server');
  assert.match(tutto, /non escon[oa]|never leave|stay on your/i, 'la home non dice piu’ dove stanno i file');
});
```

- [ ] **Step 2: Riscrivere le due frasi**

In `src/landing/copy.js`, `privacy.body`:

```js
      body:
        'Lo scontorno gira nel tuo browser. L’archivio sta nel tuo disco. I tuoi file non escono da questo computer: il server sa solo chi sei e quanto ti resta.',
```

e in inglese:

```js
      body:
        'The cutout runs in your browser. The archive lives on your disk. Your files never leave this computer: the server only knows who you are and what you have left.',
```

- [ ] **Step 3: Commit**

```bash
npm test 2>&1 | grep -E "^not ok|^# (pass|fail)" && npm run build >/dev/null
git add src/landing/copy.js test/copyLanding.test.js
git commit -m "$(cat <<'EOF'
docs: la home non promette piu' che non c'e' un server

Perche' adesso c'e'. «Non c'e' un server che li guarda, perche' non c'e' un
server» era vero fino a ieri ed e' diventato falso alla lettera: una promessa
smentita dal prodotto la trova un cliente, non noi.

Cio' che resta vero si continua a dire, ed e' la parte che conta: i file non
escono da questo computer, il server sa solo chi sei e quanto ti resta. Regge
lo stesso confronto con Canva e Adobe.

Un test lo difende, perche' la frase vecchia e' comoda e potrebbe tornare.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Al termine

- `npm test && npm run build` verdi;
- **la libreria si apre e si scarica senza abbonamento**, e un test lo difende;
- staccata la rete, uno studio pagato lavora per sette giorni;
- un webhook con la firma falsa riceve **400**, non 200;
- chi c'era già ha quattordici giorni, detti ogni giorno;
- la home non promette più una cosa che non è vera.

Restano, con la loro spec: **B2** (i crediti: saldo, preventivo, addebito, rimborso) e **B3** (le tre generazioni: Nano Banana Pro, Seedance, la voce).
